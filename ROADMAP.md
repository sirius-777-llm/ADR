# ADR ROADMAP

> ADR (Automated Documentary Rendering V8) 项目 backlog · 跨 session 持久化
> 任何 Claude session 启动时优先查这个文件 + memory/project_adr_backlog.md

Last updated: 2026-05-30

---

## 🚀 已 Ship (Recent)

| 日期 | PR/改动 | 说明 |
|------|---------|------|
| 2026-05-30 | ✅ B69 全本滕王阁序 E2E 跑通 (5 chunk → 5min02s) | 52句/5段[12,12,12,12,4] 真 motion 渲染全成: chunk1-4 全部"提前收割·跳过step10推送"(reap 生效无僵死)、BGM 跨chunk复用、xfade+acrossfade 1s crossfade 跨5段、时长校验 301.8s≈预期301.2s 通过. 单段渲染 chunk1 54min/chunk2 37min/chunk3 54min/chunk4 23min (WERYDANCE 主导). **遗留: `_tg_send_final` 用 curl+官方API sendDocument 推 82MB 失败** (官方 bot 硬限 50MB; 且 macOS LibreSSL curl 上传 bad-record-mac). 手动补救: ffmpeg 压 82MB→28MB(CRF30 720p) + Python requests sendVideo 送达 msg 21684. **待修 B69.3: _tg_send_final 改 requests(非curl) + >48MB 自动压缩后 sendVideo(非 sendDocument)**. ROADMAP B70 "sendDocument≤1900MB" 在官方端点不成立(无本地 Bot API server, 全走 api.telegram.org), 标记勘误 |
| 2026-05-30 | B69 reap 修 ADR 不退出僵死 (CCP review-code) | **根因**: ADR 干完活(成片+TG推送)后因残留非守护线程+连接池 CLOSE_WAIT **不退出**(实测卡~1h), long_video 的 subprocess.run 死等→流水线僵住 (单跑/bot 无人同步等故未暴露, B69 串行 subprocess 现形). **修**: run_one_chunk 改 `subprocess.Popen + start_new_session=True` + 轮询循环 — 最终 mp4 生成且 ffprobe 有效且大小连续两轮稳定即 `_kill_proc_group`(killpg 连 ADR 派生 ffmpeg/curl 一起杀, 防孤儿) 收割. mp4 在 step9 生成/step10 TG推送前 → 提前收割**顺带跳过 per-chunk TG 推送**(否则 TG 收 5 个残段+SSL重试拖时). 成功判定: reaped 或 exited-有效成片(rc≠0/负被信号也救回). 超时不重试 + 重跑前清 out_dir + out_dir 归属守卫. Codex review-code: 1 High(进程组 killpg)+2 Med(mp4 大小稳定防 race / wait 兜底收割)+2 Low 全修. chunk0(87min成片68.9s)抢救标 done. **实测 resume: chunk0 跳过 + chunk1 经 ADR_CHUNK_BGM_REUSE 复用 chunk0 bgm.mp3 ✓** (BGM 跨chunk复用核心特性首次实跑确认). test 45/45 |
| 2026-05-30 | B69 E2E 加固 (CCP review-code) | 滕王阁序全文(864字/52句, 古诗文网抓取剥异文注)真渲染 E2E 暴露 + 修: (1) chunk timeout 默认 1h→4h (12-scene grid_multiref+WERYDANCE 重试现实值, 实测 chunk0 撞 1h 被砍); (2) 超时不重试 (同 workload+同 timeout 重跑必再超时, 白烧一个 timeout); (3) 重跑前清空 out_dir 防上次残片污染 ADR; (4) out_dir 归属守卫 `_outdir_belongs` (rmtree 前校验 run_root/out_*, 防篡改 manifest 误删别处, Codex review Med2) + 清理不彻底告警(Med1). **fail-loud 在生产被真实触发并正确中止**(chunk0 失败未吐残缺滕王阁序, 验证 B69 核心安全设计). test_long_video.py 38→45. E2E resume 续跑中 (--timeout 21600 6h/chunk, ~10-15h 过夜) |
| 2026-05-30 | B69 长文 chunk 衔接生产级 — Phase1 code (CCP 全 4 阶段) | 新 `tools/long_video.py` 长文编排器 (进化 B66): 内容驱动 gate(句数≤30 单 run 不切, 短题材不注水) + `b69_manifest.json`(原子写+resume, Codex 方案 B 种子) + BGM 连贯(chunk0 产 bgm, 2..N 经 `ADR_CHUNK_BGM_REUSE` 复用同轨) + xfade/acrossfade 1s 平滑 concat(A/V 不等回退 -c copy) + fail-loud+retry(缺段中止不静默丢) + 边界守卫(空/序/去重/路径归属). run_adr_v8.py 2 钩子: `ADR_SCRIPT_OVERRIDE` + `ADR_CHUNK_BGM_REUSE` env(改前 backup `.bak.*_b69`). 投查修正 ROADMAP 假设: 跨 chunk meta_grid 已免费 / 字幕重编号是误解. Codex Stage3 2H/2M/1L 全修(concat 转义 / resume 注入 / crossfade A/V 漂移 / validate 中止). `tools/test_long_video.py` 35/35 + py_compile + 双分支 dry-run 全过. E2E 真长文渲染待大哥 bot. 二期 B69.x: 并行/chunk_size 自适应/衔接 prompt |
| 2026-05-30 | B68 单 run scene cap 18→30 + WERYDANCE 段长 QA (CCP 4 阶段全过) | (1) LLM 规划 prompt 范围 6~18 → 6~30 + 加 WERYDANCE 4-15s 段长引导; (2) L3624 clamp `max(6, min(18, n))` → `max(6, min(30, n))` + L3628 非 almanac fallback 9→12; (3) `ADR_GPT_IMAGE2_STORYBOARD_MAX` default 24→36, `_GRID_MAX` 32→48 (env 仍可覆盖); (4) 新增 `_b68_clamp_scene_durations_to_werydance_bounds(script, origin)` fail-fast 截断 dur 到 [4,15] + audit `voice_assets/b68_dur_clamps.jsonl` (gitignore), 调用点 step6_parallel/step65_motion/step66_adsd_lip_sync 入口. **Codex High fix**: L12172/L12969/L13067/L13074 motion clamp 上限 10→15 让长段不被截短 (下限 5 保留防 status=1002). **Codex Medium fix**: NaN/inf 用 `math.isfinite` 拦下防穿透 weryai. 单 run 撑到 2.5-3.5min, 配 B70 split 推送达 4-5min 目标. 9/9 + 4 NaN unit + Codex Stage 4 14 mock 断言全过 |
| 2026-05-30 | B70 TG 上传 oversize policy router (CCP 4 阶段全过) | step10_deliver size 判断点加 env `ADR_TG_OVERSIZE_POLICY` ∈ {compress, split, document_first} router, default compress 保留现状. **split**: `_b70_split_and_deliver` ffmpeg `-ss/-t -c copy` 按时长切 N=ceil(size/48MB) 段保 1080p 原画质, curl sendVideo 顺序推 + caption `(i/N)`, 全段成功才算 ok. **document_first**: `_b70_send_document_first` 直发 sendDocument 原片 (≤1900MB, 3 retry). 任一策略失败回退 compress. **Codex High fix**: split 失败时记录 sent_message_ids → deleteMessage 撤回已发段, 全撤干净 fallback compress, 撤不干净设 `_b70_split_partial_failure` flag fail-loud 不重复发. **Codex Medium fix**: `_b70_env_float` 防御 env (nan/inf/abc/0/负数 → default, clamp [10, 49]/[100, 1999]). E2E 10 case unit + Codex Stage 4 25 mock 断言全过 |
| 2026-05-30 | B67 stdout 敏感词遮罩 (CC content filter 防火墙, CCP 4 阶段全过) | log() 前置 _redact_for_stdout 层 14 词默认 (胡虏/匈奴/腥膻/饥餐/渴饮/秦皇/汉武/唐宗/宋祖/成吉思汗/天骄/贺兰山缺/靖康耻) + env ADR_EXTRA_SENSITIVE_WORDS 追加 + env ADR_DISABLE_STDOUT_REDACT 关闭. 命中替换 [REDACTED] + 原文 audit voice_assets/stdout_redactions.jsonl (gitignore). 防 ADR monitor 推送回灌 CC-小土伯频道触发 Anthropic 400 "Output blocked by content filtering policy" (5/29 实测满江红 235× 累积触发连环 ban). TG 推送走 _tg_send_raw 不经 log, Telegram 收到的进度仍是原文. E2E 6 case + 非 str 5 case + audit 落盘验证通过 |
| 2026-05-30 | B66 长文分段渲染 MVP | tools/long_text_split.py + tools/long_text_runner.py, 滕王阁序全文/赤壁赋等 split → multi-run → ffmpeg concat |
| 2026-05-30 | B61.1 voice clone ASR force align | faster_whisper segment-level → per-turn boundaries 重写 sub_start, 修不均匀拉伸导致后半字幕消失 |
| 2026-05-29 | B62.1 grid_multiref 末组合并法 | floor 法 group_size + loop _b62_1_ranges 末组 <2 合并到前组, 9 panel 53s → 4 group 60s 容器 ✓ |
| 2026-05-29 | B62 grid_multiref group_size 自适应 | TTS-aware optimal_n_groups + 末组合并保 ≥ 2, 9 panel TTS 38.7→3 group 容纳评书慢节奏 |
| 2026-05-29 | B60 voice clone 情感引导 | _EMOTION_NARRATION_STYLE_MAP 19→11 style + 'do not flatten' 指令注入 grid_multiref + PR-A merged_a voice_clone prompt |
| 2026-05-29 | B61 voice clone 字幕对齐简化版 | step8 入口 ffprobe scale = clone/tts, 缩放 sub_start/audio_start/vid_duration, per-turn 原子校验防半缩放 |
| 2026-05-29 | B59 use_embedded voice 1.8→2.5 | env ADR_USE_EMBEDDED_VOICE_VOLUME default 2.5 + clamp + isfinite, 修将进酒"声音太小" |
| 2026-05-29 | B56.1 IP voice 强制覆盖 LLM | 122716 实测 B56 失效 fix: _finalize_adsd_turns 删 'not turn.get(voice_asset_id)' check, IP voice 直接覆盖 LLM 选择 |
| 2026-05-29 | B56 ADSD IP voice 优先级 | _ip_voice_asset_for_speaker + _finalize_adsd_turns + _apply_llm_voice_assignment IP-locked 跳过, 修岳飞 IP 配许知远但 ADSD 选牧神记 |
| 2026-05-29 | B55 PR-A merged_a voice clone | audios 切到 voice_asset_ref.path + prompt B49 风格嵌入真台词, 修 PR-A 用 TTS 78 而 per-scene 用 慕神纪导致跨路径音色不一致 |
| 2026-05-29 | B54 _postprocess in-place guard | 修 PR-A merged_a 同名 input/output ffmpeg fail (满江红 105219 实测 B53 effective 归零的 root cause) |
| 2026-05-29 | B53 ADSD monologue 自动 batching | detect 1 unique speaker auto enable PR-A 路径合并, 修满江红独白音色漂移 (8 次独立 clone → 2 batch) |
| 2026-05-29 | PR-3e Art Director Coordinator Phase 1 | _coordinator_review 5 类规则冲突检测 + 终极锁版, brief.coordinator audit, OiiOii 7-Agent 5/7 完成 |
| 2026-05-29 | PR-3d 美术指导 Agent 规则版 Phase 1 | _rule_based_art_design 复用 DIRECTOR_STYLE_ROUTES + era 路由 (palette 5 hex/costume/props/grain/anchor) 写 brief.visual.*, PR-3d.1 prompt 注入留长期 |
| 2026-05-28 | B52 grid_multiref TTS-aware duration | _grid_multiref_duration 加 TTS buffer hint (×1.3 评书节奏), 修末句截断, 总 18s→23s 容纳 voice clone 慢节奏 |
| 2026-05-28 | PR-3c 音效师 Agent Phase 1 | _SFX_TYPE 17 类 + _audio_director_design LLM+规则兜底 + brief.audio.sfx 写入, 不动 mux (PR-3c.1 长期) |
| 2026-05-28 | PR-3b.2 5 enum 强约束 + blueprint fallback | _shot_blueprint_enums 按位置硬分配 + bp 始终当 default per-field, 修 Opus 漏 7/7 字段 + jiangwen prompt 强约束 |
| 2026-05-28 | PR-3b.1 step6/65 集成结构化分镜 | scene 字段透传 5 enum → _storyboard_grid_prompt Cinema 行 + _build_motion_video_prompt 优先 camera_motion + cinema_tail, 注入过滤 + 限长 |
| 2026-05-28 | PR-3b 结构化分镜 schema | jiangwen 加 5 enum 字段 (shot_type/camera_angle/lighting/camera_motion/visual_motif) + enum 白名单校验 + brief.shot_list 写入. step6/65 集成留 PR-3b.1 |
| 2026-05-28 | PR-3a 制片中台 creative_brief.json | OiiOii 7-Agent 升级地基. voice_assets/briefs/ schema v1 + helper 全套 + topic_decomposition mapper. 下游 PR-3b/c/d/e 填字段 |
| 2026-05-28 | B44 chat() 429 智能退避 | _is_llm_rate_limited_error helper + 30/60s 指数退避, 治 Flash Lite quota 阻断, 跟 image 1001 rate limit 不同 helper |
| 2026-05-28 | B50 speaker IP voice_asset 优先级 | 新增 _resolve_voice_asset_for_ads_speaker 三级 resolver, IP voice (曹操→慕神纪灵境道人) > podcast > fallback, 修"没有曹操音色" |
| 2026-05-28 | B49 grid_multiref prompt 嵌入真台词 | _grid_multiref_prompt 仿 _motion_audio_dub_prompt 加 'speaking exactly these lines: 「line1」...' 让 voice clone 跟字幕同步 |
| 2026-05-28 | B40.3 grid_multiref group fail retry | group 2 task_failed/poll_timeout 60s 退避自动 retry 一次复用 payload, 修 stochastic WERYDANCE fail 阻断 combined 写入的 root cause |
| 2026-05-28 | B40.2 step7 grid_multiref 保留 audio | step7 STORYBOARD_GRID_MULTIREF_MAIN 删 -an, pad 用双 -i + apad 静音补尾防 voice 重复, B40/B40.1/B40.2 三连闭环 |
| 2026-05-28 | B40.1 step9 grid_multiref 检测 | step9 use_embedded 加 grid_multiref_motion_qa.json 优先分支, 修 NoneType.get crash 让 B40/B35/B25 audio 链路真正生效 |
| 2026-05-28 | B43 Flash Lite Tier 缩小 | review tier Flash Lite → Flash 2.5 (WeryAI 不支持 Haiku 撤回 data 部分), L3088 沉浸感 QA 调用点改 tier_chat |
| 2026-05-28 | B33 step6 错峰 12→5s | 3 处默认 stagger 12→5s, retry 路径不动, 429 自动退避 cover, 单 run 省 42s-2min |
| 2026-05-28 | B39 封面异步 worker retry | _async_kickoff_cover_caption worker 加 3 attempt retry 15s/30s 退避 · _await 超时置 aborted=True 防竞态 · caption 兜底防 step10 crash · 字段预留 B30 状态机 |
| 2026-05-28 | B41 HADS pipeline_state 保存 | step6 后无条件 _save_pipeline_state，rerun_downstream 现在支持 HADS resume（之前只 ADSD 分支存）|
| 2026-05-28 | B40 grid_multiref concat 保留 audio | _grid_multiref_concat_paths normalize 不再 -an，保留 voice clone audio (易中天评书腔)，避免 fallback master TTS 78 女声 |
| 2026-05-20 | silent_b PR | a_roll/narrated_b/silent_b 三类 turn 数据模型 + LLM prompt 改造 + step9 BGM 动态浮起 |
| 2026-05-20 | Bug A-E fix | BGM 平衡 / per-part loudnorm / 字幕 ASR 对齐 / BGM aloop / 字幕 SUB_GAP |
| 2026-05-20 | 人设符 PR (character_meta_grid) | 4×3 中文标签 grid 取代切片 panel，召唤式 prompt |
| 2026-05-20 | 4 个减时间方向 + bug fix | skip sidecar QA / character_sheet / storyboard_grid / meta_grid 缓存 |
| 2026-05-21 | Speaker IP Card | speaker 升级为完整角色档案，8 个 seed (曹操/刘备/司马懿/旁白/科比/乔丹/鲁迅/辜鸿铭) |
| 2026-05-21 | Speaker IP A (AI 孵化) | tools/create_speaker_ip.py，给名字+简介自动生成完整 IP + meta_grid，已测杜甫 |
| 2026-05-21 | Speaker IP B (自学习) | usage_history 跑后回写 IP，下次 LLM 注入摘要避免重复 |
| 2026-05-21 | Speaker IP C (关系网络) | IP 加 relationships 字段，5 个种子角色补上 (曹操/刘备/司马懿/科比/乔丹)，script-gen prompt 注入关系上下文 |
| 2026-05-21 | 横屏字幕换行 fix | _wrap_card max_width 14.5 → SUBTITLE_MAX_CHARS+2，避免 LLM 切的 16 字段被强制 \N 换行 |
| 2026-05-21 | BGM vocal 自动检测 + retry | _bgm_contains_vocals whisper ASR 检测，含 lyrics 自动重试 (WERYAI music 偶尔忽略 "no vocals") |
| 2026-05-21 | resume tool --regen-bgm flag | 单独重生成 BGM，其他物料复用，针对 BGM 含 vocal 等场景秒修 |
| 2026-05-21 | Era LLM template (主路径) | _llm_infer_meta_grid_template + _resolve_meta_grid_template，LLM 推断 era + 4 类标签集，写入 IP cache 跨片复用，修「南京银行出古装」bug |
| 2026-05-21 | ERA_TEMPLATES 预设 + 兜底链 | 7 个 era 预设 (historical/contemporary_corporate/modern_athlete/modern_scholar/future_tech/...) + costume/pose 智能匹配 |
| 2026-05-21 | LLM 阶段 1 topic_decomposition | _llm_topic_decomposition + topic_cache，一次 LLM 出 era/bgm_style/role/director/cover/is_action_topic 多字段 |
| 2026-05-21 | LLM 阶段 2 BGM 接入 | generate_bgm 用 LLM 推断 bgm_style + instruments + mood，替换 8 硬关键词分支 |
| 2026-05-21 | LLM 阶段 3 role/director/action 标记 | _adsd_role_candidates / _storyboard_grid_prompt / script-gen 输出 is_action_scene 字段 |
| 2026-05-21 | Spike action 武戏天花板验证 | 4-panel multi-ref + kinetic prompt + 10s 武戏密度明显比 baseline 强（大哥确认）|
| 2026-05-21 | action_b turn_type PR (4b) | 第四类 turn (武戏)：4-panel multi-ref + duration 10s + kinetic prompt + LLM 自动标 |
| 2026-05-21 | 非 ADSD action 强化 (阶段 5) | _motion_action_block 检测 is_action_scene → 注入 kinetic prompt |
| 2026-05-21 | duration 计算优化 | api_dur 下限 5s → 3s + tts_dur+0.3 buffer，短 dialogue 处理时长降 30-40% |
| 2026-05-21 | IP 版本管理 (I) | ADR_IP_SCHEMA_VERSION="1.1" + _migrate_speaker_ip 加载时自动迁移，9 个 IP 已 v1.1 |
| 2026-05-21 | IP 调用统计 + 推荐 (F) | schema v1.2 加 usage_count + 跑后自动 increment + _recommend_related_ips + tools/show_ip_stats.py CLI |
| 2026-05-21 | NO TEXT IN FRAME 补强 | _adsd_almighty_audio_dub_prompt + _adsd_meta_grid_call_prompt 加英文 ABSOLUTELY NO TEXT IN FRAME ban，修易筋经 00:48 内嵌字幕 |
| 2026-05-21 | action_b 题材触发优化 | script-gen prompt 鼓励武侠/对抗题材主动出 ≥1-2 个 action_b（之前易筋经 0 个 action_b 因 LLM 保守）|
| 2026-05-21 | LLM 智能召唤标签 | script-gen LLM 同时推 meta_grid_costume/pose 字段 + scene 注入 IP costume/pose 池，_infer_* 优先读 LLM 标签，规则兜底 |
| 2026-05-21 | AUTO-IP · ADR 主流程自动孵化 | step1 后扫描脚本无 IP speaker → LLM 孵化（含 voice_asset 智能匹配 + 全字段填充 + qa_status=auto_generated_pending_review）·env ADR_AUTO_INCUBATE_IP 控制 · 实测苏东坡 ✓ |
| 2026-05-21 | duration=3 bug fix (P0) | duration 优化下限 3 越过 WERYDANCE 硬限 4，整 run 39min 挂掉；回收下限 → 4 + 双保险 max(4) · 实测笑傲江湖 turn 5/9 status 1002 root cause |
| 2026-05-21 | step7 防御层 | 缺失 seg 时用 img_path 现场补 still seg + 不可恢复时从 script 移除 turn，避免 ffprobe 整 run 挂掉 |
| 2026-05-21 | P0 action_b 标注 bug fix | _infer_turn_type line 451 武戏命中错返 narrated_b → 改 action_b + _is_action_shout 短喊招检测（武侠/体育/战争 全覆盖）+ 兜底收紧 ≤12 字防误标长教学 dialog · 13/13 case 通过 |
| 2026-05-21 | P1 武戏密度 LLM 化 | topic_decomposition 加 action_density_hint (low/medium/high) + recommended_action_b_count + script-gen prompt 按密度给 min/max action_b 硬指标 + 武戏 ≥3 必须连续 2 个组成节拍 · 笑傲江湖实测 high/4 |
| 2026-05-21 | AUTO-IP 占位符过滤 hotfix | _auto_incubate_missing_ips 跳过 (silent)/(action)/silent_b/action_b/none + 场景描述类 speaker（>8 字 含场景/对话/画面等词），防误孵化垃圾 IP |
| 2026-05-22 | WERYDANCE FAST retry 兜底 | 每个 variant family 末尾追加 2 次 WERYDANCE_2_0_FAST 重跑 · env ADR_WERYDANCE_FAST_RETRY_COUNT 控制 · 单 turn FAST 尝试 1→3 次 · 验证: 科比 attempts 数 3-4 → 5-6 |
| 2026-05-22 | almighty task_failed 诊断信号 | 从 data.msg 抽 reason 入 attempt 记录 + 不再 filter response · 现在 task_failed 看到具体「Content moderation / Image asset audit / Copyright restrictions」原因 |
| 2026-05-23 | step7 Ken Burns + 3 层降级 | 缺 seg 时 Ken Burns slow-zoom 兜底（1.2× scale 防 ffmpeg 超时）+ Ken Burns 失败降级静态图 + 静态也失败才丢弃 turn · ffmpeg 失败后 cleanup partial · 笑傲江湖 + 科比都验证 |
| 2026-05-23 | B4 武戏 SFX | action_b variants generate_audio="true" + _adsd_action_b_motion_prompt 加 SFX_DIRECTIVE (combat SFX only, no dialogue/music) + 跳过 leading silence trim + _build_voice_clone_hybrid_audio 保留 action_b 内嵌音色 · 西游记 turn 4 (孙悟空) 1 action_b SFX 进 hybrid ✓ |
| 2026-05-24 | meta_grid 重构（无文字版）| 参考 strength04_x editorial 风格：panel 内 100% 纯图无文字 + 召唤改 panel index + 位置描述 (top-left/middle-right/...) 取代「曹操｜战袍｜说话」中文标签 · 彻底解决 WERYDANCE 复刻 ref 文字进画面的根本 bug · 西游记验证完美: 0 fallback / 2 action_b SFX / 视频零内嵌字幕 ✓ |
| 2026-05-25 | tools/quality_audit.py | 达尔文进化适应度评估器 · 扫所有 /tmp/adr_v8_*/lip_sync_qa.json + pipeline_state.json · 输出每 run 通过率/SFX 数/fallback/audit 触发词 · 趋势曲线 + markdown 报告 · 支持 --last N / --md |
| 2026-05-25 | 批量清理旧版 cached meta_grid | 删 34 个含中文标签的旧 PNG (backup 在 /tmp) + 清 7 个 IP 的 meta_grid_template_cache · 留 4 个西游记新 grid · 让所有 IP 下次都享受新无文字 grid 红利 |
| 2026-05-20 | TG deliver 假阴性 fix | status=200 后验证 body {"ok":true,"result":{}} |
| 2026-05-20 | tools/rerun_downstream.py | 跳过 step1-66 跑下游，10x 调试加速 |

---

## ⏳ Planned (按优先级)

### 🎬 长视频突破 4-5 min (2026-05-30 议定 · 当前单 run ~90s 是甜点上限)

> **背景**: 大哥反馈"现在视频不到 90 秒，希望进化到 4-5 分钟"。诊断 5 重收紧瓶颈：(1) LLM 规划分镜数 `max(6, min(18, n))` 硬上限 18 scene × 平均 5-7s ≈ 90-126s；(2) **WERYDANCE 单次 duration ∈ [4, 15]s 硬限**（weryai API 端，run_adr_v8.py:15018 注释，下限 4s 是实测 — duration=3 → status=1002 整 run 挂；上限 15s — API 端拒不更长），意味着长视频只能"加段数"不能"加段长"；(3) GPT_IMAGE2 storyboard panel 24 / grid 32 上限超过即跳过，质量塌；(4) WERYDANCE 每 turn 一次 lip-sync 18 turn ≈ 18min 渲染，扩到 30 turn → 30+min + gateway 504 风险指数上升；(5) TG sendVideo 50MB 单文件硬限，4-5min 1080p 必爆。
> **路径**: B68 单 run 撑到 ~3min (18→30 scene, 段长仍守 [4,15] 上限) + B69 B66 多 chunk 自动 concat 拼到 5-6min + B70 TG 上传降级链 (sendDocument 2GB fallback)。三者组合可在不彻底改架构前提下达到目标。
> **⚠️ 铁律 (2026-05-30 大哥定)**: 长视频能力**不强制**。视频长度必须由**内容丰富度**自适应决定 —— 短诗就该短，丰富长文才撑到 4-5min。cap 30 是天花板不是目标，短题材**严禁注水**(重复镜头/拖节奏)。L3601-3607 规划 prompt 已是内容驱动 (短诗 6-12 / 中等 14-20 / 长文传记 20-30)，静态确认符合。B69 chunk 衔接**只对真实长文** (滕王阁序全文等 800-3000 字) 触发，绝不给短题材凑时长。详见 memory/feedback_adr_length_by_content.md。

| 项目 | 工程量 | ROI | 状态 | 备注 |
|------|--------|-----|------|------|
| ~~**B68 单 run scene cap 24-30 + 两段式 LLM 规划**~~ | ~~4-6h~~ | ~~★★★ P0~~ | 🟡 shipped 2026-05-30 (CCP 全 4 阶段, MVP 30min 简化版) | 实际 ship MVP: cap 18→30 + storyboard max 抬升 + scene dur QA fail-fast clamp + motion clamp 上限 10→15 (Codex High fix). 详情见顶部已 Ship 区. **延后到 B68.2 长期**: 两段式 LLM 规划 (outline→detail) + WERYDANCE batched submit + 自动拆合 scene (不只 clamp). Codex Stage 1 共识"30min 简化版 fail-fast 不自动修"采纳 |
| **B68.2 两段式 LLM 规划 + scene 自动拆合 (长期)** | 4-6h | ★★ P2 | 🟡 **OBSERVE 增量 shipped 2026-06-03; B-act+A planned** | **recon+design (workflow wwxk1gqmj) 诊断**: 头重脚轻(narrative-allocation)与 clamp hit(duration-fit)是**解耦的两个问题** → A 两段式只修头重脚轻, B 拆合只修 clamp. **架构师定序: B-observe → B-act → A** (clamp hit 已 instrument 可测 + 截断是静默质量损伤更急 + B-observe 零渲染风险 + A 依赖 observe 分布数据). **已 ship B-observe**: env `ADR_SPLIT_PLAN_OBSERVE`(默认关) 在 `_b68_clamp` 的 if clamps 块内从 clamps(原始 dur)派生拆合候选(from>15→split ceil/from<4→merge)写 audit, 不改 script 零渲染风险, 攒生产分布数据. CCP review-code 中. **剩**: B-act `ADR_SCENE_AUTO_SPLIT`(新 `_b68_split_long_scenes`: dur>15 拆 2+按字符比例/LLM 重分台词, dur<4 merge 邻居, 重分 audio/sub timings) + A 两段式(L3745 后插 outline LLM 调用 6-8 acts × detail, 修头重脚轻; 高 blast-radius 改 spielberg/jiangwen prompt, 最后做). 原触发: B68.1 cap 30 叙事塌/clamp 命中率高 |
| **B69 长文 chunk 衔接 (生产级)** | 6-8h | ★★★ P0 | 🟡 Phase1 code shipped 2026-05-30 (CCP 全 4 阶段), E2E 待大哥 bot 验证 | **硬约束(内容驱动铁律)**: chunk 只对真长文触发, 短题材单 run 自适应, 绝不凑时长. 新 `tools/long_video.py` 编排器: (a) 内容驱动 gate — 句数 ≤ single_run_max(30) 走单 run 不切; (b) `b69_manifest.json`(Codex Stage2 方案 B 种子)记每段句范围/status/资源/验收, 原子写 + resume; (c) BGM 连贯 — chunk0 先跑产 bgm.mp3, chunk2..N 经新 `ADR_CHUNK_BGM_REUSE` env 复用同轨(接 step6_parallel `pregenerated_bgm_path`); (d) concat 默认 xfade+acrossfade 1s 平滑(A/V 流长不等则跳回 -c copy), 按 manifest 校验段数/时长; (e) fail-loud+retry — 单 chunk 超时+1 次重试, 仍失败中止整片不静默丢; (f) 边界守卫 — 空 chunk/严格顺序/去重/路径归属(防 manifest 注入). run_adr_v8.py 2 钩子: `ADR_SCRIPT_OVERRIDE` env(并行铺路) + `ADR_CHUNK_BGM_REUSE` env. **投查修正 ROADMAP 假设**: 原 #1 跨 chunk meta_grid 已免费(speaker 级缓存跨 run 自动复用), 原 #3 字幕重编号是误解(每 chunk 烧录 concat 自动保留). **Codex Stage1**: ROI 确认+4 边界(audit/dup/empty/timeout)+分期建议; **Stage2**: 方案 B 流式编排, 共识融合(A 落地+audit 当 manifest 种子); **Stage3**: 2 High(concat 转义+resume 注入)/2 Med(crossfade A/V 漂移+validate 中止)/1 Low 全修. 35/35 unit + py_compile + 双分支 dry-run 全过. **二期 B69.x**: 并行 chunk/chunk_size 自适应/衔接 prompt(仅 LLM 生成台词题材). 实施边界: 本机无 WeryAI/TG key, 真 E2E 长文渲染留大哥经 bot |
| ~~**B70 TG 上传降级链 (sendDocument 2GB fallback)**~~ | ~~2-3h~~ | ~~★★ P1~~ | ✅ shipped 2026-05-30 (CCP 全 4 阶段) | env `ADR_TG_OVERSIZE_POLICY` ∈ {compress, split, document_first} router. split 保 1080p 切段 + 失败 deleteMessage 撤回, document_first 原片 sendDocument ≤1900MB. Codex High fix 残片撤回 + Medium fix env 防御. ship 详情见顶部已 Ship 区 |
| ~~B68.1 单 run 30 scene 实测对比~~ | ~~1h~~ | ~~★★~~ | 🟢 静态结案 2026-05-30 (原设计推翻) | **原"强制 18/24/30 三档"既错也做不到**: (1) env 只是下游 gate 控制不了分镜数, 唯一强制精确分镜数靠脚本注入但那绕过了要验的 planner, 自相矛盾; (2) 按"内容决定长度"铁律, 固定档位对比本身违背原则. 静态结论: L3601-3607 prompt 已确认内容驱动 (cap=天花板, 短诗导向 6-12, 无上限偏置), 唯一强制是老黄历固定 22 句 (结构固定合理). 真要经验验证应跑"丰富度梯度题材谱"(短诗→单事件→中等→传记→长文, 各 1 run) 看分镜数随内容单调缩放 + 短题材不注水, 而非固定档位. 配套缺口: 渲染计时未落盘 (只推 TG), 需加 run_timings.json. 当前判定: 静态已够, 不烧重型渲染, 转 B69 |
| B69.1 B66 chunk 并发上限调优 | 30min | ★ | planned (blockedBy B69) | parallel chunk run 并发数 (2/3/4) 对比 weryai gateway 504 率与总耗时, 找拐点 |
| **B69.2 长文衔接二期 (Phase 2)** | 部分 shipped | ★★ | 🟡 并行 chunk ✅ shipped 2026-06-01 (CCP S1+S3); chunk_size 自适应/衔接 prompt 仍 planned | (1) **并行 chunk run ✅**: long_video main chunk0 先串行(产 BGM+voice master)→ barrier → chunk1..N `ThreadPoolExecutor(max_workers=--parallel default3)` 并发, 各 chunk 独立 override/output dir(B69 铺路), manifest 写 threading.Lock, fail-loud as_completed 收集不 cancel(保留在飞 chunk 供 resume), resume 补抓缺失 master. CCP S3 High(override_file 归属守卫)+Med(manifest 半更新低危)处理. 实测 7h→~2-3h(真实加速看 weryai 并发容忍, 撞 504 降 --parallel, 见 B69.1). py_compile+45/45+dry-run(--parallel 4). **真 E2E 并行多 chunk 渲染待大哥下次长视频跑**. (2) chunk_size 自适应 + (3) 衔接 prompt(仅 LLM 台词题材)仍 planned. |
| ~~**B73 跨 chunk 音色锁定**~~ | ✅ shipped 2026-06-01 (CCP S1+S3, code-only 不重出旧片) | ~~1-2h~~ | 实现: run_adr_v8.py L4673 后读 env `ADR_CHUNK_VOICE_REUSE` 校验库内+speech_safe → 覆盖 ads_voice_asset_id 绕过 LLM pick (ADSD 不锁, 失败回退); long_video.py manifest 加 voice_master, chunk0 done 读 pipeline_state script[0].voice_asset_id 锁定 → run_one_chunk env 注入 chunk1..N. CCP S3 High/Med 全无, Low(ADSD 不捕获)已补. py_compile+45/45+梁文道库校验通过. | 滕王阁序 B69 长片实测(大哥反馈"03:32 音色突然不是梁文道"): **每 chunk LLM 独立选 voice_asset** → chunk0-2 梁文道(liyan3)/chunk3 易中天(pingshu-c7c18f5a)/chunk4 罗翔, 跨段换 3 个旁白(不是漂移是选了不同人). root: 各 chunk 独立跑 `_resolve_voice_asset_for_ads_speaker(first_speaker, picked_id)`(L4673)按各自 LLM picked_id 解析. **修(仿 BGM 复用)**: chunk0 跑完锁定其 voice_asset_id → 新 env `ADR_CHUNK_VOICE_REUSE` 注入 chunk1..N 在 L4673 voice 解析前强制覆盖. **现有视频**: voice 烧在 motion(grid_multiref audio_dub), 改 voice 需重渲 chunk3+4(~2h). 配 CCP |
| ~~**B74 整片连续 BGM bed**~~ | ✅ shipped 2026-06-01 (CCP S3 全过, code-only) | ~~2-3h~~ | 实现: run_adr_v8.py step9 入口 env `ADR_CHUNK_NO_BGM`→bgm_path=None 走纯人声分支(已存在); long_video **仅 chunked 模式**各 chunk 设该 env 渲纯人声(chunk0 仍 step6 生成 bgm.mp3 供 bgm_master), 拼接后 `_overlay_continuous_bgm` 叠一条连续 bed(aloop 环绕+volume 0.4+amix weights 照搬 step9, -c:v copy+faststart 仅重混音频). CCP S3: High(single_run 不该强制 no-BGM→加 mode==chunked 守卫)+Med(overlay 加 +faststart)已修. 已验证 py_compile+45/45+overlay 真跑(66.4s 有效)+step9 no-BGM 日志生效. | 大哥反馈"全片 BGM 不连贯". root: B69 BGM 复用成功(5 段都用 out_00/bgm.mp3 32s)但 step9(L17475)每 chunk `aloop` 从 0:00 起 bed → **每段边界 bed 重启**, long_video crossfade 只平滑音量不修音乐重启. **修**: 剥每 chunk BGM(raw_concat 本就纯人声无 BGM), 拼接时叠**一条连续 bed** 按累计时长 offset-seek(chunk_N 起点 = cumdur % bgm_dur)+ aloop 环绕. **可免重渲**(用各 chunk raw_concat 重烧字幕 no-BGM → concat → overlay 连续 bed). 配 CCP |
| ~~**B75 字幕 word-level ASR 对齐**~~ | ✅ shipped 2026-06-01 (CCP S3 全过, code-only) | ~~3-4h~~ | 重构 `_b61_1_asr_turn_boundaries`: `word_timestamps=True` 收 word 级 (start,end), **char 比例**(按各 turn 字数加权)映射 turn 边界, 取代 segment-index 比例(忽略段内静音 gap 致 gap 后漂移). 无 word 回退 segment-index(旧行为), 失败回退 B61 均匀 scale. caller 传 valid_texts. 已验证: chunk1 重跑 step8 "B75 word-level align 182 words→12 turns 1.50~73.42s", 末字幕 73.42s(B72 覆盖不丢). CCP S3 High 无, Med(旧 faster_whisper 不支持 word_timestamps→except 兜底回退)优雅降级. | B72 修了字幕覆盖/结束时间, 但大哥反馈"配音字幕仍不同步". root: B61.1 `_b61_1_asr_turn_boundaries`(L16452) 用**比例映射** 17 ASR 段→12 turn(floor/ceil 索引), **忽略 ASR 静音 gap**(实测 chunk1 37.48→40.72s 有 3.24s gap)→ gap 后 turn 起点漂移累积. **修**: `word_timestamps=True` 取 word-level Whisper 时间戳, 按 turn 文字 char overlap 映射真实语音时序取代比例映射. **可 reburn 验证**(step8, 免重渲). 影响所有 voice clone 视频. 配 CCP |

### 角色库扩展

| 项目 | 工程量 | ROI | 状态 | 备注 |
|------|--------|-----|------|------|
| ~~A · AI 自动孵化 IP~~ | ~~2h~~ | ~~★★★~~ | ✅ shipped 2026-05-21 | tools/create_speaker_ip.py |
| ~~B · 自学习 usage_history~~ | ~~1.5h~~ | ~~★★★~~ | ✅ shipped 2026-05-21 | _record_speaker_usage_history + prompt 注入 |
| ~~C · 角色关系网络~~ | ~~1h~~ | ~~★★★~~ | ✅ shipped 2026-05-21 | IP relationships + _build_speaker_ip_context_for_script |
| ~~AUTO-IP · ADR 主流程自动孵化 IP~~ | ~~1h~~ | ~~★★~~ | ✅ shipped 2026-05-21 | step1 后扫描自动孵化 + qa_status 待审标记 |
| D · 角色弧线版本 | 3h | ★★ | planned | 曹操_青年/中年/晚年 多版本，LLM 看时代自动选 |
| E · 多人物互动 IP | 4h | ★★ | planned | 双人卡 (曹操+刘备) 同框 IP，含对峙/煮酒姿势 |
| ~~F · IP 调用统计 + 推荐~~ | ~~1h~~ | ~~★★~~ | ✅ shipped 2026-05-21 | usage_count + _recommend_related_ips + show_ip_stats.py |
| G · IP 管理 CLI 工具 | 2h | ★ | planned | tools/ip_manager.py 列出/编辑/创建 IP |
| H · IP 质量评分 | 2h | ★ | planned | LLM 评估视觉/音色/性格契合，写 IP 跟踪 |
| ~~I · IP 版本管理~~ | ~~30min~~ | ~~★~~ | ✅ shipped 2026-05-21 | ADR_IP_SCHEMA_VERSION + _migrate_speaker_ip |

### LLM 化 + 武戏强化 (2026-05-21 议定，5.5-6.5h 总)

| 项目 | 工程量 | ROI | 状态 | 备注 |
|------|--------|-----|------|------|
| ~~阶段 1-5 (LLM 化 + 武戏)~~ | ~~5.5h~~ | ~~★★★~~ | ✅ shipped 2026-05-21 | 全部 6 阶段已 ship |
| ✗ image-to-video 升 almighty | - | - | rejected | 保留作 fallback (endpoint 冗余容灾) |

### 减时间优化

| 项目 | 工程量 | ROI | 状态 | 备注 |
|------|--------|-----|------|------|
| ~~**PR-A · merged_a 合并跑**~~ | ~~4-5h~~ | ✅ shipped 2026-05-27 (CCP 全 4 阶段, MVP default-off) | 同 speaker 连续 turn 合并 almighty 调用, 砍 30-50% API · 实施 3 部分: (1) L12618 `_concat_audio_files_for_group` + `_split_lip_sync_raw_by_durations` helpers (ffmpeg concat demuxer + 按 dur 切片重编码) (2) L13089 `_lip_sync_one_group` 主函数 (拼接 audio + 提交 almighty audio_dub generate_audio=true + poll + split + 各段 postprocess; 失败返回 [False]*n 让调用方回退) (3) L13700 step66 主循环加 group/scene 分流提交, group 失败时 fallback 走单 turn _lip_sync_one_scene · 触发条件: ADSD_CONSECUTIVE_SPEAKER_BATCHING + audio_dub_experiment + multi-turn group + all A-roll + total_dur ≤ 15s · CCP Stage 3 抓 3 个 High/Medium: split 边界用 actual audio dur / 15s 二次校验用 actual_total / generate_audio='true' 对齐单 turn · Default OFF 需 --adsd-speaker-batch 显式启用, 待大哥实测验证后再考虑改默认 |
| meta_grid 缓存预热 | 30min | ★★ | planned | 跑 driver 给常用 speaker 预生成 meta_grid 入缓存 |
| ~~0.5s submit interval 重测~~ | ~~5min~~ | ~~★★~~ | ❌ rejected 2026-05-27 (PM 重评估) | 计算: 12 turn run 总 motion+image submit 共 22 次, 每次省 (2.5-0.5)=2s, 总省 ~44s. 全程 wallclock ~22min, 实际提速 ~3%. ROI 远低于 ★★ 标记. WERYDANCE 单镜 processing ~25-90s 才是瓶颈, submit interval 在并发模型下影响极小. 不 ship, env ADR_*_SUBMIT_INTERVAL 仍可手动 override 测试 |
| ~~duration 计算优化~~ | ~~30min~~ | ~~★★~~ | ✅ shipped 2026-05-21 | api_dur 下限 5s→3s + tts_dur+0.3 |
| ~~voice_asset upload 缓存~~ | ~~1h~~ | ✅ shipped 2026-05-26 (CCP) | _upload_to_weryai 入口加 sha256 + cache lookup (24h TTL) · double-check locking 防并发冷缓存 dup (codex final-qa 发现) · voice_assets/upload_cache.json 跨 run 持久 · 减少 SSL 抖动失败概率 · env ADR_UPLOAD_CACHE=0 关闭 |
| WERYDANCE_2_0_FAST 主路径 | 30min+测 | ★ | planned | A/B 验证 FAST 质量是否够，省 30-40% step66 时间 |

### 其他方向

| 项目 | 工程量 | ROI | 状态 | 备注 |
|------|--------|-----|------|------|
| ~~LLM 智能召唤标签~~ | ~~1.5h~~ | ~~★★~~ | ✅ shipped 2026-05-21 | script-gen LLM 同时推 meta_grid_costume/pose 字段 |
| ~~action_b LLM 标注 bug~~ | ~~1h~~ | ~~★★~~ | ✅ shipped 2026-05-21 | _infer_turn_type 修正 + _is_action_shout + density LLM 化 |
| 题材泛化验证 | 30min/题材 | ★ | planned | 跑科比/鲁迅/苏东坡/钱学森等验证 IP+人设符稳定性 |
| resume 工具 e2e test | 1h | ★ | planned | tools/rerun_downstream.py 加自动测试套件 |
| **B42 resume --regen-grid-concat** | 30min | ★★ | planned | rerun_downstream 加 --regen-grid-concat flag: 删 grid_multiref_combined.mp4 + grid_multiref_norm_*.mp4 后强制重调 _grid_multiref_concat_paths，让 step65 内部 mux bug (如 B40 audio strip) 不需重跑全 step1-65 也能验证。类比 --regen-bgm。配套可加 --regen-images / --regen-step66 覆盖其他冻结阶段。验证场景：HADS grid_multiref 模式下改 concat/normalize 逻辑 |
| ~~**B43 Flash Lite Tier 路由重分散 (缩小版)**~~ | ~~★★ P2~~ | ~~10min~~ | 🟡 shipped 2026-05-28 (CCP Stage 1+2+4, 缩小版) | Codex Stage 2 修正认知: WeryAI 不支持 Haiku (L1666 hotfix 2026-05-27 已撤回过 CLAUDE_4_5_HAIKU). 缩小到 review tier Flash Lite → GEMINI_25_FLASH (一行改 + 注释), data tier 保持 Flash Lite 等 B44 兜底 · 同时补 L3088 沉浸感 QA 调用点改 tier_chat('review') 真正生效 (B40.1 一起 ship) · 收益减半: 仅 L3085 沉浸感 QA 一次/run 迁出 · Codex 共识: B44 chat() 429 智能退避才是主解, B43 只是辅助减压 |
| ~~**B44 chat() 429 智能退避**~~ | ~~★★★ P1~~ | ~~1h~~ | ✅ shipped 2026-05-28 (CCP 全 4 阶段) | Flash Lite quota 治本 (观沧海 4 run 均触发 'Resource has been exhausted'). 新增 L1466 `_is_llm_rate_limited_error(exc)` helper 识别 quota/429/exhausted/TPM/RPM 等 LLM 限流文案 (跟 image 1001 rate limit 区分). L1463 `_LLM_RATE_LIMIT_BACKOFF` env 默认 30 (try/except + max(1.0) clamp 防坏值). L1767 chat() retry loop 区分 is_quota_err: 是则 30s/60s 指数退避 (2^attempt), 否则原 3s/6s. log/audit 加 '[B44 429]' tag. Stage 3 codex 抓 2 Medium (200 字截断丢 429 → full_resp_text 完整校验 / env 校验 → try/except clamp) + 2 Low 接受. 9/9 unit test 全过 |
| ~~**B50 speaker IP voice_asset 优先级**~~ | ~~★★★ P1~~ | ~~40min~~ | ✅ shipped 2026-05-28 (CCP 全 4 阶段) | 大哥 143918 反馈"没有曹操的角色音色" root: L4253 _podcast_id_to_voice_asset(picked_id) 忽略 IP voice_asset_id (曹操.json=external_mushenji_lingjing_001) · 修: 新增 L884 `_resolve_voice_asset_for_ads_speaker(speaker, podcast_id)` 三级 resolver (IP > podcast > fallback) · `_GENERIC_NARRATOR_NAMES` 11 名 (旁白/解说/narrator/host 等) 跳 IP 走 podcast · IP voice 校验 _voice_asset_is_speech_safe + 在 _load_voice_assets() 库 + try/except 异常 fallback podcast · L4253 改调 resolver, log/tg badge 🎭 (IP) / 🔒 (podcast) audit · Stage 3 codex 抓 Medium (data.get('assets') 可能 None → 加 or [] guard) + Low (旁白-曹操 子串/lowercase, 接受边际) · Stage 4 10 case unit test 全过 |
| **B51 voice clone reference 质量提升** | 2-4h | ★★ | 🟡 长参考+候选回退 shipped 2026-06-02 (CCP S1+S3); ASR真台词率检测/seed 仍 planned | **沁园春实测根因**: HADS grid_multiref 把旁白切 N 组(WERYDANCE 输出 15s 限), 各组独立用同一 **9s 短参考** clone → stochastic 漂移 → **单片多音色**(罗翔 9s ref × 4 组). **已修(长参考)**: (1) `_build_combined_voice_reference` ffmpeg concat asset 全部 valid ref(罗翔 4×9s=36s)→长参考缓存(per-run, repo 内路径守卫 Codex Med); (2) `_select_voice_asset_reference` mode==motion+`ADR_VOICE_COMBINED_REF`(def1)+>1ref→长参考 path + `fallback_path` 短参考(adsd 不动零回归); (3) grid_multiref 提交候选 [长,短] 依次试→都拒才 silent(避免退化 master TTS). **实测结论 (沁园春重跑 2026-06-02)**: 36s 长参考被 WERYDANCE **拒** → 4 组全 `single_fallback` 回退 9s(候选回退零退化验证 ✓)→ **确认 WERYDANCE 15s 硬限卡的是参考本身, 不只输出**. 长参考路基本死(双 15s 限: 输出+参考). **补救**: helper 改截 ≤14s(env `ADR_VOICE_REF_MAXLEN` def14)让拼接参考 <15s 被接受(14s vs 9s 多 5s 音色信息适度降方差, 但 N 组独立 clone 漂移仍在=**非彻底修**). **彻底修待深挖(都不确定)**: 多参考 `audios=[多段9s]`(需 WERYDANCE 支持多音频参考)/ 固定 seed / 后处理音色归一. |
| **B45 tier_chat() 自动降级链** | 4-5h | ★★ | planned | Flash Lite 429 修复 B 方案 (B43+B44 不够时再考虑): `tier_chat("data")` Flash Lite 失败 → 自动 retry on Haiku 4.5; `tier_chat("review")` → 自动 retry on Flash 2.5; 直接 chat() 的 4 点 (L3563/L3358/L3310/L9290) 包装 tier_chat. B43+B44 实测 1-2 周看 Flash 2.5/Haiku 是否够稳再决定 |
| ~~**B76 封面绕过重试包装器扛不住 6001**~~ | ★★ P2 | 30min-1h | ✅ **shipped 2026-06-03 (CCP S1+S3, 无 High)** | **修**: 新增 `_is_transient_workflow_error`(6001/6002/工作流系统异常 谓词) + `submit_text_to_image` 对其有限退避重试(原只认 1001 限频, 故封面光改走包装器不够—包装器对 6001 也不重试是新发现) + 封面 `_generate_cover_image` 改走 submit_text_to_image(max_attempts=2, 删重复 wait_slot/inject) + B39 worker 3→2(防 worker×submit 双层叠爆, 上限 2×2=4) + ADR_COVER_TIMEOUT 非整数守卫. codex review-spec 确认"普通单图靠外层熬 6001, storyboard/封面无外层会漏". review-code 无 High, Medium(异常分支补 6002)已修. py_compile OK, E2E 验证中. **原现象**: **沁园春实测 (2026-06-02): 封面 B39 worker 3/3 失败 status 6001 工作流系统异常**. 投查纠正(大哥指出图片接口没报错): **6001 是网关/工作流层提交时即时返回(~5s)的 transient, 没创建图片任务 → 图片 API 当然无失败记录**. 根因: `_generate_cover_image` L18679 **直接 req_post 绕过了 `submit_text_to_image`(L1568)那个带 5 次重试的包装器** —— 普通图片(img_N/storyboard/character_sheet)都走包装器, transient 6001 重试 5 次熬过去; 封面裸 req_post 拿不到 task_id 立即 None. B39 异步 3 重试也是裸 req_post 赶上 6001 窗口全挂. **修(干净)**: `_generate_cover_image` 改走 `submit_text_to_image` 包装器(自动重试+rate-limit检测), 跟普通图片一致. **附**: 封面是异步(与 step6-9 并发)+ 3 重试~60s 早早失败, step10 await 不真卡 600s — 我先前"step10 卡封面"判断也是错的, transient 失败=快速失败+无封面交付. **待确认**: 大哥最初观察的"卡在这里"若是长卡, 可能是另一问题(ADR 不退出僵死等), 需 run 日志再查. |
| **B77 Flash Lite thinking 吃爆小 max_tokens (MAX_TOKENS 空响应)** | ★★ P2 | 30min | **shipped 2026-06-02 ✅冒烟过** | **沁园春实测: 同 run 12 次 finishReason=MAX_TOKENS 空响应, 9 次"底部注脚生成失败"**. 网关把它包装成 `{status:500,"Server error"}`, 真因藏在 `message: Gemini response is empty (finishReason=MAX_TOKENS)`. 根因: **底部注脚 L18100 `tier_chat("data", max_tokens=200)` → data tier 路由到 GEMINI_3_1_FLASH_LITE, 这是 thinking 模型, 推理 token 计入 maxOutputTokens** —— 注脚正文才 7-15 字, 但 200 token 被 thinking 先吃光, 没吐正文就撞 MAX_TOKENS → choices 空. 对比同 run `max_tokens=3000` 的 Lite 调用(L2975)、字幕断句(8句)全成功 → 不是模型坏, 是小 budget 被 thinking 吃爆. **不致命**(注脚有 4 级 fallback, 回退默认注脚, 封面照出), 属日志噪音+注脚降级. **跟封面 6001(B76)是两回事**. **方案 (大哥选 3)**: 注脚改用非 thinking 的 `GEMINI_25_FLASH`. **blast radius (workflow wwhhggec8 + 对抗 reviewer 已摸清)**: data tier 共 3 个 `tier_chat("data")` 调用 → ①L18100 注脚(200) ②**L9316 voice_id 选择(max_tokens=80, 同 bug 里最毒, 静默 fallback candidates[0] 误选音色, 与多音色问题相关)** ③L3505 topic rewrite(200, 低危, 40-80 字输出). 5 个直连 Lite 调用(L2975/3568/3618/3825/10446)max_tokens≥1000 不是同 bug. **已修 (2026-06-02)**: L18100 → `chat(GEMINI_25_FLASH,max_tokens=600,timeout=45)` + L9316 → `chat(GEMINI_25_FLASH,max_tokens=300,timeout=30)`, 系统提示词原样, 未动 _LLM_TIER(定向 bypass). **L3505 延后**(低危, 留 ROADMAP). 证据: 2.5-Flash 日志里从不撞 MAX_TOKENS, 600-token 调用(L1895/L8715)全成功, 非 thinking 模型; 切换还卸 Lite 429 压力(合 B43 方向). 语法 OK. **CCP review-code 过**(codex 无 High/Medium; Low: L9319 `a["voice_id"]` 坏 catalog KeyError → 已改 `a.get(voice_id)` 过滤). **真实 API 冒烟过**: 注脚 2.5F/600 → "银装北国·江山入画·寥廓苍茫"(STOP), voice_id 2.5F/300 → luo_xiang_001(STOP), 对照旧 Lite/200 复现 500 finishReason=MAX_TOKENS 空响应. final-qa 收尾中. |
| **B78 渲染计时 instrumentation (run_timings.json) + 30min 预算** | ★★ P1 | 30min | **shipped 2026-06-03** | 支撑大哥目标"每次生成 wall-clock ≤30min". 发现 main() **已有 timings dict 逐 step 记录**(只 TG 不落盘). 新增 `_write_run_timings` 落盘 `OUTPUT_DIR/run_timings.json`(per-step 秒数+总计+主导步骤+over_budget 判定), 成功/异常两路都写. env `ADR_RENDER_BUDGET_MIN`(默认30). **内容驱动铁律: 超预算只标记+TG ⚠️ tag, 不中止**(富内容本就该长; wall-clock 由 B69.2 并行 chunk 压). TG 耗时统计加 ✅≤预算/⚠️超预算 tag. **30min 可行性 (recon wabbufmuq 量化)**: 短内容(8镜)22.76min ✅, 中等(12-16镜)30-35min 临界, **富内容(18+镜)45-60min ✗** — WERYDANCE motion 占 65-75% wall-clock 是瓶颈. **下一杠杆 (B79 待评)**: 富内容单 run 进 30min 需并行 motion 提交/poll 或 WERYDANCE_FAST 主路径 或 render-budget-aware 自适应. py_compile OK, E2E 沁园春(本次规划 20 镜)实测验证中. |
| **B79 并行 grid_multiref motion 组 (富内容进 30min 核心杠杆)** | ★★★ P1 | 2-4h | 🟡 **code shipped 2026-06-03 (CCP 全 4 阶段, 默认关), 待并行 E2E 实测** | **已实现**: `_generate_grid_multiref_motion_segments` 组处理体抽成嵌套 `_run_grid_group`, env `ADR_MOTION_GROUP_WORKERS`(默认 1=旧串行零回归; >1=ThreadPoolExecutor 有界并发). 关键: 提交由现有 `_wait_motion_submit_slot` 全局节流→并发组提交仍串行节流(不撞 1001/504), **只 poll 真并发=提速核心**. 并发安全: record 处理期线程局部, 完成才 `_finalize()` 锁内一次性 append+写QA(消半状态竞态), resume 读加 `_grid_multiref_tasks_lock`, B40.3 retry 走节流. CCP S1(codex 认可先2并发)+S3(High 半状态竞态/2Med 锁/Low 全修)+S4(并发复核: 竞态消除/workers=1等价旧串行/_finalize不漏不双; mock 8组覆盖 resume/no_task/异常/retry). py_compile OK. **基线实测 (沁园春 20镜 串行, run_timings.json)**: 总 **81.4min**, 动态化 WERYDANCE **64.7min=79%** 铁证瓶颈(图片+BGM 9.2min/TG 3.5min/其余<2min). **关键发现**: 默认 HADS 主 motion 路径是**逐镜 motion-audio-dub**(非 grid), grid_multiref 是 sidecar → **B79 并行的 grid 组路径需 `GRID_MULTIREF_PRIMARY=1` 才是主路径**. **B79 验证 E2E 进行中** (`GRID_MULTIREF_PRIMARY=1 ADR_MOTION_GROUP_WORKERS=3`, 本次15镜): 看 grid 主路径+3并发能否把 motion 从~65min→~8min 进 30min. **B79 验证 E2E 结果 (2026-06-03, 15镜 grid-primary+workers=3)**: 日志实锤 `B79 并行 motion: 7 组 × 3 workers`, motion **64.7→36.0min(降~45%)**, 总 81→48.7min. **但仍 >30min**: grid 主路径部分回退逐镜 motion-audio-dub(串行)+ 本次 weryai 网络抖动重(多 ConnectTimeout/ReadTimeout/ConnectionReset)拖时. 质量绿(封面140s/15图全过/字幕15句). **结论: B79 打通并行 grid 砍 motion 近半但富内容 ≤30min 未坐实**, 顽固瓶颈=逐镜 motion-audio-dub 串行. **→ B79.1 调查推翻"并行逐镜": step65_motion 已 `ThreadPoolExecutor(max_workers=min(20,n))` 20 路并发! 64.7min 不是串行, 是 weryai 渲染吞吐硬限(~3.2min/镜服务端排队), 再并行无用**. **唯一保 ≤30min 的 ADR 侧杠杆=减 render 数**. **B79.1 已 ship (code, env `ADR_RENDER_BUDGET_SCENE_CAP` 默认关)**: `_apply_render_budget_scene_cap` 按预算 cap 分镜(budget30→~8镜), LLM+fallback 过 cap, almanac(22句结构刚需)+外部override 结构豁免(黄历 ≤30min 需 FAST 非削句). CCP S3(High almanac覆盖/Med fallback+override未cap/Low 已修—almanac结构豁免, fallback已加cap)+S4. 实锤沁园春 24→8镜. **封顶 E2E(8镜)验 ≤30min 中**. **结论(诚实)**: ≤30min 短/中内容(≤~8镜)早达标; 富内容(15-20镜)受 weryai 吞吐硬限不能既富又 ≤30min → 要么 cap 削句(env开)要么 FAST 模型(B-FAST, 需A/B)要么按铁律接受其长. 默认关=尊重内容驱动. **pillar ④ ≤30min: 能力已具备(cap 开关), 富内容 vs ≤30min 是 weryai 吞吐物理张力**. **✅ 实测坐实 (2026-06-03 封顶 E2E)**: 沁园春 cap 24→8镜 → 总 **81.4min(基线串行)→ 30.0min(降63%)**, motion 64.7→23.1min, 质量全绿(封面158s/8字幕/推TG). 卡 30.0min 微超 → per_scene 默认 2.5→**3.0** 留余量(预算30→7镜~26min 稳进). **ADR_RENDER_BUDGET_SCENE_CAP=1 即保 ≤30min 且全自动+高质量+审美在线**. final-qa 全 PASS. 现 `_generate_grid_multiref_motion_segments`(L13601) `for group_no in _b62_1_ranges` **串行**: 每组 submit+poll 跑完才下一组, 20 镜≈6-7 组×~150s 串行=17-21min motion(占总 65-75%). **修**: 仿 step6(ThreadPoolExecutor poll_workers=20)/B69.2(并行 chunk) 把组提交+poll 改**有界并发**(默认 3-4 workers, env 可调), motion wall-clock 从 sum(组)→~max(组)+提交stagger, 富内容 40min→~20min 进 30min. **风险**: WERYDANCE 并发撞 1001 限频/网关 504(见 B69.1 并发拐点), 需保留每组 B40.3 retry+退避 + 有界并发. **替代/叠加**: WERYDANCE_FAST 主路径(省30-40%/镜, 需质量A/B) + render-budget-aware 自适应. 走 CCP 4 阶段(改 motion 核心编排, 高风险). |
| **B91 导演级运镜 (Seedance 2.0 范式: 结构化 camera grammar + 内容感知 + 复合运镜)** | ★★★ P1 | 2-4h | 🟡 **B91.1 ✅ shipped 2026-06-05 (CCP S3+S4), 余分批** | **⚡ 关键修正 (大哥)**: `WERYDANCE_2_0` 就是 Seedance 2.0——ADR 一直在用 Seedance, 只是没吃满它的能力(原生结构化运镜语法/参考视频/原生音频 generate_audio 都暴露在 almighty/i2v/t2v payload 里). 故 B91 不是"近似 Seedance", 是"解锁 ADR 没喂满的 Seedance 原生能力". **▸B91.1 ✅ shipped**: 新增 `_SEEDANCE_CAMERA_GRAMMAR`(11 camera_motion enum→Seedance `Camera:[move]+speed+subject-lock+stability` 完整导演句)+ `_seedance_camera_directive(scene)`(enum 映射 + 情绪门控复合运镜: 紧张/震撼/高潮+push-in/static→dolly-zoom 眩晕; str 强制防崩; 非法兜底); wire 进 `_build_motion_video_prompt`(L12104)HADS+ADSD 两路, 把"Camera motion: crane up"单词升级成"Camera: crane boom rising upward, smooth, steady, revealing scale". CCP S3(Med×2 ADSD in-set/move 非str崩→_validate_enum_field+str()修, Low×2 死代码删+dolly-zoom关键词收窄防惊喜误触发)+S4 PASS. 13 case 单测全过. 纯 prompt 零 API 改动, 回归极低. **来源**: seed.bytedance.com/seedance2_0, fal.ai/seedance-2.0, 10b.ai/promeai camera 指南. | **起因**: 大哥发 Seedance 2.0 推文(@alvinfoo) /goal 研究新玩法增 ADR 能力. **研究结论**: Seedance 2.0(ByteDance, 2026-02 发布, 4月 API public beta)核心新玩法=①参考视频复刻运镜(@reference 上传clip→复刻相机轨迹/速度/构图变化/转场)②结构化运镜语法 `Camera:[move]+[speed]+[subject-lock]+稳定度+镜头` 9种+复合(dolly-zoom眩晕)③时间轴多镜(0-4s远→4-8s推→8-12s环绕 一次生成)④原生音频帧级同步⑤多模态引用(9图+3视频+3音频 @角色指派). **ADR 后端核查**: weryai 只有 WERYDANCE/almighty(reference-to-video/image-to-video/text-to-video), **无 Seedance/Kling**; 且 Seedance API beta(限频/breaking/法务-名人deepfake被起诉), 官方评测"不适合生产级自动化管线"→**不直接接**. **ADR 现状 gap (已 grep 核实)**: camera_motion 已是 LLM 每镜从 11-enum 选(L4489, 非纯硬编码, _shot_blueprint_enums L2327 仅 fallback); `_build_motion_video_prompt`(L12104)已把它注入 HADS 路径 WERYDANCE("Camera motion: slow push-in"); **但** (a)只单值 enum 缺 Seedance 结构化语法(speed/subject-lock/stability/复合); (b)ADSD 对话路径(_adsd_broll_motion_prompt L14625 等)用通用 "handheld documentary realism" **没读 camera_motion**=运镜没被导演; (c)无复合运镜(dolly-zoom/orbit-push). **B91 方案(用现有 WERYDANCE, 零新依赖零法务风险, 把 Seedance 导演哲学落地)**: ▸B91.1 结构化运镜语法: LLM 运镜导演每镜出 `Camera:[move]+[speed]+[subject-lock]+[stability]+[lens]`(Seedance 语法)替代单值 enum, 内容感知(配 shot 动作/情绪/B89 custom_visual_style.rhythm); ▸B91.2 ADSD 路径接 camera 导演(现通用 handheld→按 turn 内容定运镜); ▸B91.3 复合运镜入库(dolly-zoom 情绪高潮/orbit 揭示, emotion/action 门控); ▸B91.4(选)almighty-reference-to-video + 运镜参考片库(Seedance @reference 概念, 需建 clip 库); ▸B91.5(选)timeline 多镜语法测 WERYDANCE 是否吃. **不做**: 原生音频(ADR TTS+voice-clone 是核心)/直接接 Seedance(beta+法务). 走 CCP, final-qa 跑对照题材验运镜多样性+不崩. **来源**: seed.bytedance.com/seedance2_0, fal.ai/seedance-2.0, 10b.ai camera-movement-control, promeai camera cheat-sheet. |
| **B90 封面生成器独立于风格系统 + 篡改题材内容 (中华万年历 ink-wash 硬模板)** | ★★★ P1 | 1-2h | 🟡 **planned (赛博朋克验证 render 暴露)** | **2026-06-04 赛博朋克验证 render 实测**: 正片帧 B89.4 custom 正确出 neon 赛博(义体医生 grid=义体机械臂/全息屏/霓虹城实锤), **但封面把"赛博朋克2099记忆黑客"反框成古代水墨"庄周化蝶·古代记忆哲学"+挂"中华万年历"banner**(同百乐门"中华万年历串味"但更重——不只风格不符, 还篡改题材内容: neon 科幻→古代文人水墨). 根因: 封面路径(`_generate_cover_image`/almanac 系封面模板)独立于 B89 风格系统(director_tag/route/texture_mode/custom_visual_style 全不读), 且疑似有 topic→古典化 rewrite. **影响**: 封面是第一眼, neon 正片配古代封面=观感精分; 各种非黄历题材都挂"中华万年历"=串味. **修(待 CCP)**: 封面生成读 B89 单一风格契约(base_style/route anchor/custom_visual_style/texture_mode), 与正片同风格; 去掉非黄历题材的"中华万年历"硬 banner + topic 古典化 rewrite; 黄历题材才走 almanac 模板. 归入 B89.2 单一视觉契约收敛. |
| **B89 让 LLM 决定视频风格、适应各种题材 (风格系统全面去一刀切, B87 范式推广)** | ★★★ P0 | 多批 (3-5天) | 🟡 **P0-1 quick-win shipping 2026-06-04, 余分批** | **目标(大哥 /goal)**: llm决定adr视频的风格能适应各种不同的题材. **审计 workflow(8 agent 并行审 6 维 + 16 题材探针 + 综合, 等价 CCP S1+S2)发现**: B87 已把"纹理"一维解放给 LLM, 但风格系统其余维度仍是 4 类反模式(封闭枚举/塌默认黑洞/dead字段/全局硬皮). **真正根因(比 director_route 7枚举更致命)**: `L4812-4818 director_tag 全局硬皮`——`style_key = "editorial magazine photography series, {base_style}, warm cream and sage green palette, soft natural daylight, 4k photorealistic..."` 作 `parts[0]`(L4877)最高权重前缀贴**每张图、连 tone 都不读**, 把所有题材色板/介质无差别洗成"奶油+鼠尾草绿杂志风"(B87 去脏后缀的放大版). 且: producer STYLE_KEY 自由文本从不回注 / art_director brief.visual.palette+style_anchor 只被 QA warn-only 读=dead / route palette 只进文字 prompt 不进出图 style_key. 探针最不适配题材: 儿童童话/热血体育/恐怖/赛博朋克/深夜美食(全塌 modern_documentary+sage green). **分批修复计划 (B87 范式: 受约束提名→代码护栏veto→单点收口)**: ▸**P0-1 (quick-win) ✅ shipped 2026-06-04 (CCP S3+S4)**: 删 L4813-4815 无差别硬皮(editorial magazine + sage green + 4k photorealistic), director_tag 回落自适应 `base_style`(topic_meta.director)+ route palette(轻松→明快色, codex Med 修) + 题材无关连贯锚. **+ 双胞胎收口**: final-qa 揪出 jiangwen 一致性铁律(L4403/4411/4412)还硬压 Kinfolk杂志+sage green+`never neon/cool blue`(掐死赛博/恐怖/夜景)——改为指向自适应 STYLE_KEY、不锁 medium、不禁霓虹冷蓝(保一致性约束本意). 立解全片色板/介质全局污染. py_compile+ship_log. ▸**B89.2 (P0)**: producer STYLE_KEY 自由文本→受约束 medium 枚举提名(editorial_photo/storybook/ink_wash/cyber_macro/high_key_food/noir/cartoon...)+ `_style_key_guardrail`(tone/题材冲突 veto)+ 接回 art_director style_anchor 单一数据源. ▸**B89.3 (P0) tone 解放**: 5档死记+塌中性 → 自由 descriptor + 15 情绪原语(_EMOTION_NARRATION_STYLE_MAP 现成)提名向量 + 拆"轻松==现代==儿童"耦合(体育被塞轻松儿童档是生产 bug), 扇出 6 子系统分批. ▸**B89.4 (P1) director_route ✅ shipped 2026-06-04 (CCP S3+S4)**: 加 custom 逃生口——decomp schema 加 `| custom` 枚举 + `custom_visual_style{anchor,palette,rhythm}` 字段 + 规则7 custom 说明(7预设不贴合→自拟); `_validate_custom_visual_style` 护栏(anchor≥8/palette≥4/类型 isinstance(str)/长度上限/禁导演画家实名中英); `_resolve_route_style`(preset / custom自拟 / 兜底modern 可观测log) 统一供 `_director_route_block` + L4818 palette. 恐怖→noir/科幻→cyberpunk neon/美食→微距/儿童→明快 等落不进 7 桶的题材拿 LLM 自拟风格而非塌 modern_documentary, 破 7 枚举天花板. CCP S3(High route 未 str() 包裹崩→修 + Low anchor/palette 类型校验→修, Med ensemble cap 压5 可接受)+S4 PASS(13 case 单测: 护栏/解析/block/非字符串route/list anchor/None decomp 全兜底不崩). 待补具名新桶为 priors 留 B89.4b. **B89.4a hotfix (2026-06-04)**: schema 那行 `"custom_visual_style": {...}` 在 decomposition **f-string** 里用了单花括号→运行时 `ValueError: Invalid format specifier`(py_compile 过/helper 单测过都没抓到, 只有真跑 decomposition 才炸——赛博朋克验证 render 逼出). 修=花括号转义 `{{...}}`. 教训: CCP codex 是看描述审非跑码 + 单测没构建 f-string=盲区; 已补"实际构建 decomposition prompt"的端到端测试. ▸**B89.5 (P1) BGM**: 兜底 culture-aware(去 erhu 默认)+ 1002 重试保题材 family. ▸**B89.6 (P1) motion/pacing**: 接回 route.rhythm 到 motion + LLM 提名 pacing_profile(快剪/慢推/恐怖hold). ▸**B89.7 (P2) art_director**: dead 规则版→删/或 LLM brief 接回出图(culture 进决策修古埃及→汉服). **每批走 CCP final-qa 跑对照题材(儿童/体育/恐怖/赛博/美食)防回归**. |
| **B88 战争/灾难题材 ADSD TTS 旁白撞 weryai 1002 敏感内容 → 改写软化不够 + 耗尽硬崩整管线** | ★★★ P0 | 1-2h | ✅ **shipped 2026-06-04 (CCP 全 4 阶段)** | **✅ 修**: (1)`_ADSD_POLICY_REWRITE_TERMS` 扩 14 个军事/暴力词(日军→那支军队/炮火→战火/轰炸→空中袭扰/屠杀→重大伤亡等); (2)`_rewrite_adsd_tts_text_for_policy` 加 `level`(1-4 clamp)逐级加狠: L1软化语气/L2删军武暴力专有词/L3抽象历史化/L4极中性, 每级铁律"绝不新增事实只弱化抽象现有内容"防 LLM 幻觉; (3)新增 `_tts_safe_fallback_line(tone)` 极中性兜底句(庄重/怀旧/诗词古文/轻松/中性 5档)+ `_tts_silent_placeholder` ffmpeg anullsrc 静音 + **纯 Python wave 兜底**(ffmpeg 失败时 wave.open 写 44100/mono/16bit/2s 合法 wav, 强校验非空, 绝不返坏路径破坏永不崩); (4)`_tts_turn_to_audio` 重试循环 for→while 重构: **内容审核(1002)→分级改写(env `ADR_TTS_MAX_REWRITES` 默认4)→中性兜底句→静音占位, 全程绝不 raise**(replace-turn 保 turn 数/时间轴/lip-sync 不变); 网络/transient 错另走有限退避重试耗尽才抛(不同类). **CCP**: S1(P0先止崩再分级) + S2(codex 偏 LLM 上下文域, 采纳"保原文审计链") + S3 review-code(High `_tts_silent_placeholder` 吞 ffmpeg 失败返坏路径→wave兜底修 + Med 静音漏审计→补 `_record_adsd_tts_rewrite`) + S4 final-qa PASS(High闭合/回归低/战争题材可安全跑完). **验证**: py_compile过 + 5组单测全过(safe_fallback按tone/rewrite分级clamp+军事词典/全1002永不崩escalation L1-4→中性兜底→静音/中性句过审出audio不到静音/网络错有限重试后抛不无限) + ffmpeg强制失败实测 wave 产出 176KB 合法 wav. **起因**: **2026-06-04 跑 hadsd 淞沪会战(B87 film_grain 验证)实测**: turn1 旁白"1937年8月，日军的炮火，第一次落在上海街道"→ `text-to-audio` 返回 `status 1002 检测到敏感内容`. 现有 `_tts_turn_to_audio`(L5698) 有 policy rewrite 机制 + `_rewrite_adsd_tts_text_for_policy`(L5242) 调 GEMINI_25_FLASH 改写, **但**: (1)改写提示词(L5246)只说"避免控诉/暴力/煽动", **没让删军队/武器专有词(日军/炮火)** → 2 次改写都留着"日军炮火"仍 1002; (2)函数**无状态**, 不会逐次加狠; (3)rewrite 上限只 2(L5742 `rewrite_count < 2`), **耗尽 `raise RuntimeError`(L5758) 抛到 step2_dialogue_voice→main → 整个 ~30min 管线崩 exit 1, 不出片**. **违反"合法题材绝不崩整管线"铁律**(同 motion"绝不退静止→剔除turn"). 跟 B83(BGM 1002)同类但在 TTS 旁白路. **B87 本身验证通过**(崩前日志实锤 LLM 提名 film_grain→护栏 film_grain, 民国未被误水墨), 崩在无关的 TTS 审核. **修方案 (待 CCP)**: (a)`_rewrite_adsd_tts_text_for_policy` 加 level 参数**逐级加狠**: L1 软化语气 → L2 删军队/武器/暴力专有名词(日军→那场战事/炮火→战火硝烟) → L3 抽象历史化(去掉一切军事/国家/政治具体词) → L4 极中性兜底; (b)rewrite 上限 2→4 配合 escalation; (c)**graceful degradation 铁律**: 全部耗尽**绝不 raise 崩管线**, 改用保证过审的极中性兜底句(LLM"完全不提军队/武器/暴力/国家政治, 只抽象复述情绪"+硬编码安全常量句兜底), 保 turn 数/时间轴不变继续出片; (d)扩 `_ADSD_POLICY_REWRITE_TERMS` 覆盖军事词. **取舍**: 软化战争旁白会损史实直接性(日军炮火→战火), 但 weryai TTS 审核是硬约束, 软化保出片 > 崩. 走 CCP 4 阶段. |
| **B87 题材纹理/颗粒由 LLM 美术总监决定 (texture_mode) + 收口散落 grain/sepia 硬注入** | ★★★ P1 | 3-5h | ✅ **shipped 2026-06-04 (CCP 全 4 阶段, 大哥"按你建议")** | **✅ 已实现**: 新增 `_TEXTURE_MODE_ENUM`(clean/film_grain/ink_wash_aged/aged_paper/print_halftone) + `_TEXTURE_SUFFIX_MAP`(clean=原后缀逐字零回归, 每档自带去数字脏噪 negative, film_grain 保 `fine 35mm film grain` 只杀 digital noise/oversharpen, ink_wash/aged 显式 `no film grain/no sepia photo/no halation` 对抗残留) + `_era_is_pre_photographic`(摄影年代逃生舱: republic/民国/晚清/清末/近代/含≥1850年份→False, token边界{tang..qing}→True; **关键修正: 见'qing'但有 late/民国/年份不判前摄影, 保住老上海/民国怀旧颗粒**) + `_texture_mode_fallback` + `_texture_guardrail`(优先级: 非法→clean / 轻松→clean / 近现代 photographic→clean / 诗词古文→ink_wash / 前摄影 veto→ink_wash / 空 era+photographic+非怀旧→看水墨证据否则 clean) + `_set_active_texture_profile`(env `ADR_TEXTURE_PROFILE` 覆盖) + `_ACTIVE_TEXTURE_PROFILE` 模块全局(step1 每片重设). decomp schema 加 `texture_mode` 字段+规则8, cache 版本 5→6. step1 tone 后设全片纹理档(almanac 强制 clean). **Stage0 收口 (texture_mode 成颗粒/做旧唯一所有者)**: L4249/L11148 正文老照片sepia→`_active_texture_body_directive`, L11728 对话场景→`_active_texture_scene_phrase`, meta_grid film grain→`_active_texture_grid_phrase`, 4处 motion bridge+2处动作路径(L392武侠/L14532极限)film grain→`_active_texture_motion_phrase`, L2409 镜头蓝本硬拼35mm grain→纹理档, L4438 producer战争指南删grain, L8992/L8990 classical_realism route删sepia/archive-photograph, L4623 jiangwen fallback sepia→纹理档, L4238 producer STYLE_KEY加纹理权威条款(勿自写grain/sepia). 保留: L1083 era-aware菜单/1919封面sepia(era正确)/BGM配乐/封面tone-map怀旧→sepia/封面装饰. **CCP**: S1 review-spec(确认真修·只改后缀是假修·Stage0必做) + S2 propose-alt(codex 收敛到 LLM导演+规则护栏=本方案) + S3 review-code(High×2 动作路径硬编码grain + Med 幂等裸短语误判 → 全修, 幂等改完整后缀 endswith 零误判) + S4 final-qa(抓 classical_realism route 残留 sepia + 同步 → 已修). **验证**: py_compile过 + 14 case 单测全过(民国/晚清/老上海保film_grain未被误水墨/唐诗veto/盛清前摄影→水墨/现代clean/空era保守/跨档幂等/env覆盖) + 5档头尾相干性自检过. **起因**: 大哥问"为什么题材视觉风格不能让 LLM 角色决定?" + 怀旧/民国题材想要胶片颗粒却被磨平. **design workflow (4 独立方案+对抗评审, agent 逐行 grep 验证) 挖出真相**: ①`GPT_IMAGE2_QUALITY_SUFFIX`(L1561, 注释写明"大哥 2026-05-20 给的固定 suffix")本是修 GPT_IMAGE_2 默认脏感的一刀切补丁, 无差别 `-- no grain` 追加到每条 prompt 尾部, 副作用碾压故意做旧颗粒; ②**颗粒/sepia 入口不止那条后缀**——还有 8+ 处 body 级硬注入绕过瓶颈: L4249(tone!=轻松→"老照片 sepia tone")、L4222(producer→historical heavy film grain)、L9742(meta_grid 母版硬写 cinematic film grain 且不读 brief.visual)、L8766/8768(classical_realism anchor archive-photograph+film grain)、L1928/1948(detect_topic_meta director 默认 historical sepia)、L14225(motion bridge 无条件 film grain)、L2075/2103/2212(1919 封面 sepia); ③**正文说 sepia/grain 与尾部说 no-grain 同时存在=自相矛盾**, 纹理实际不可控; ④`_infer_art_director_brief` 已出 `grain_level`(L8447)但是**硬编码 route map(L8428)查的、且 dead 字段从没注入 prompt**(L8354 注释"Phase 2 长期"未做). **方案(D 骨架+B 缓存+强制 Stage 0 收口)**: LLM 在已缓存的 `_llm_topic_decomposition` 顺手吐受约束枚举 `texture_mode`{clean/film_grain/ink_wash_aged/aged_paper/print_halftone}(零额外 LLM 调用) → 纯函数 `_texture_guardrail` 确定性否决(前摄影 era 禁 film_grain 改判 ink_wash; 轻松/现代/儿童强制 clean) → `_TEXTURE_SUFFIX_MAP` 枚举翻后缀(每档自带去脏 negative, clean 档=现后缀原文逐字保零回归) → 落 topic_decomposition cache 跨 run 复现. **Stage 0(先决·不可跳)**: 把上述 8 处 body 硬注入全部条件化为 texture_mode 驱动 + texture_mode 透传进 meta_grid 路径(L9669/L9742, 防带颗粒的人设母版污染 speaker_ips 跨片复用)——只改后缀不收口正文=假修复, 前摄影题材照样被糊. **护栏**: era 空值兜底"不许胶片"(宁丢水墨纹理不糊胶片); 复用 L8524 token 边界 re.split(防 shanghai 里 han 误伤); 幂等 guard L1574 'clean illustration' 改多档 marker 检测(防 motion bridge 二次贴重/漏); 模块级 texture 状态每片入口 reset(防 evolve_runner batch 串档); env ADR_TEXTURE_PROFILE 覆盖钩子. **关键修正(对抗评审挑出)**: 晚清1900s/民国1920s 是真摄影年代, 护栏不能见'清'就 token 命中 qing 强制水墨(那会把大哥要的老上海/民国怀旧颗粒错削)——qing 仅当不含 late/末/民国/老上海 才判前摄影. **取舍待大哥拍**: (a)真修(Stage 0 收口 80-150 行 touch 2 个 LLM 角色+meta_grid+motion bridge) vs 只改后缀(便宜但前摄影仍被正文糊)——大哥"不考虑成本考虑质量"→倾向真修; (b)全片单档 vs scene 级纹理(v1 单档, scene 级留 B87.1); (c)cache 版本 5→6 整体失效重推一次. **风险**: Stage 0 漏一处(尤其 L9742 meta_grid)前摄影人物镜头仍带颗粒+污染 speaker_ips; era 字段高频空(L1947 fallback)绕过护栏→需并看 culture/period_visual/STYLE_KEY 关键词. **走 CCP 全 4 阶段**, final-qa 专测"正文 grain 与后缀 no-grain 是否仍共存"(非 happy path). **替代原 B87 硬 gate**(对抗评审判其只把碾压从 1 档变 3 档、管不到 8 个正文源、era 正则脆性维护债, 该弃). |
| **B80 GEMINI_3_1_FLASH_LITE 被 weryai 下线 (P0 生产级)** | ★★★ P0 | 20min | ✅ **hotfix shipped 2026-06-03 (CCP S3, 无 High/Med)** | **跑荤段子 HADSD 时崩**: `chat(GEMINI_3_1_FLASH_LITE)` 返回 `status 1006 "The model does not exists"` 3 次重试全挂 → ADSD 对话生成(L3000)RuntimeError 崩整管线. **本 session 内突然移除**(几十分钟前沁园春 capped run 还能用). **live 探活**: Lite 及所有 Lite 变体(GEMINI_3_FLASH_LITE/GEMINI_25_FLASH_LITE 等)全 1006/1002, **仅 `GEMINI_25_FLASH`(+CLAUDE_4_6_OPUS)可用**. **修**: 5 个 `chat(GEMINI_3_1_FLASH_LITE)` 调用(ADSD对话L3000/情感L3676/分镜规划L3726/旁白L3936/BGM描述L10576)+ `_LLM_TIER["data"]` 全换 `GEMINI_25_FLASH`(唯一可用 Flash). **副作用**: 25_FLASH 非 thinking → **顺带根除 B77 类 thinking-MAX_TOKENS 病根**(整条 data 链不再有 thinking 模型). 2 处历史注释保留. 运行引用 0. CCP review-code 无 High/Med(Low: L3530 topic rewrite 短输出低危). HADSD 重跑越过崩溃点验证生效(Step1✓/ADSD音色分配✓). 影响所有 ADR run(Lite 是核心 LLM). **大哥指 `GET /v1/chat/models` 可查全 28 模型**: 查出 **Lite 竟在列表里(out=7.5 最便宜)但 chat 调用 5/5 全 1006 → 列表不可靠**; 同价 out=15 的 `GEMINI_3_0_FLASH`(Preview)/`GEMINI_25_FLASH` 都可用, 选 25_FLASH(已反复验证+非Preview稳). **B80.1 self-heal shipped**: chat() 检测 `status 1006`/"does not exist"/"not model" → 自动 fallback 到 env `ADR_CHAT_FALLBACK_MODEL`(默认 GEMINI_25_FLASH)+continue 重试, 防同 model(避免无限切), 让未来 weryai 无预警改模型不再崩整管线. 冒烟3例正确(1006切/普通500不切/fallback自身不切). |
| **B81 质量优先模型升级 (台词/旁白/视觉→最强 Claude)** | ★★ | 30min | ✅ **shipped 2026-06-03 (大哥: 不考虑成本考虑质量)** | 新增 `CREATIVE_MODEL`=env `ADR_CREATIVE_MODEL`(默认 `CLAUDE_4_8_OPUS`=列表最强 Claude out=1250, 可改 GEMINI_3_5_FLASH/GPT_5_5). **升 5 处质量关键调用**: `_LLM_TIER["producer"]` + ADSD 对话台词(L3016, timeout 180→300) + 斯皮尔伯格旁白(L3952, +timeout300) + 制片文化准则(L4025) + jiangwen 视觉导演(L4337) 全 4_6_OPUS/25_FLASH→CREATIVE_MODEL. **机械档不动**(分镜数/情感/BGM描述/voice_id JSON/data tier 留 25_FLASH—要可靠 JSON, premium 模型啰嗦反伤解析; creative/review tier 也不动—call site 超时太短会噎死 Opus). 探活确认 4_8_OPUS/GPT_5_5/3_5_FLASH 全可用. py_compile 过, CCP review-code 中. **下次 render 生效**(台词/叙事/视觉质量↑, 慢+贵↑, 大哥要质量). 起因: 大哥问"chat 为啥不用 GEMINI_3_5_FLASH"→查价 3_5_FLASH out=500 是 25_FLASH 的 33x(premium 档), 故创意档升最强 Claude、机械档留便宜可靠的. |
| **B84 上传重试加硬 + B85 motion 失败 cascade 图生视频 (绝不退静止)** | ★★★ P1 | 1h | ✅ **shipped 2026-06-04 (CCP S3+S4)** | 大哥铁律: WERYDANCE almighty/图生/文生三接口在, **坚决不退 Ken Burns 静止**. 鹦鹉一镜静止 root: ①图片上传撞 weryai 网络错(本session SSL bad-record-mac/ConnectionError 持续抖)②step66 ADSD lip-sync(almighty)失败直接掉 step7 Ken Burns, 没 cascade 别接口(step65 per-scene 早已 cascade almighty→图生→文生). **B84**: `_upload_to_weryai` 重试 3→6(env ADR_UPLOAD_RETRIES, 非数字 try 兜底)+ 网络错(ssl/timeout/connection/reset/bad record/aborted)8-40s 长退避熬过抖动窗口; 上传是所有 motion/voice ref 前置→广泛防失败. **B85**: 新 `_rescue_motion_image_to_video`(上传+WERYDANCE image-to-video+_poll_video_task_download), step7 seg 缺失先调它拿**真动效**, ken_burns_success=True 即跳过静止; 仅 i2v 也失败才退 Ken Burns(真最后兜底). env ADR_MOTION_RESCUE_I2V 默认开. review-code Med 修: 救援 poll 限 max_iterations=45 单次(~3.5min)不拖批量 + 只救场景自己非grid图(邻/cover 走 Ken Burns, animate 错图比静止更糟). 鹦鹉静止已烧死需重渲消除. **B86 (2026-06-04, 大哥: ken burns 不要换 text-to-video)**: 删 step7 Ken Burns `_render_still_segment` 调用+静态降级链, 改 `_rescue_motion_text_to_video`(WERYDANCE text-to-video 用 scene prompt 不吃图任何场景能救). **完整 motion cascade: almighty→image-to-video→text-to-video→剔除turn(绝不静止)**. env ADR_MOTION_RESCUE_T2V 默认开. review/final-qa 修: vp 空守卫(防下载 Path("")) + 空队列 hard-fail(全 drop 时清晰中止不崩 ffmpeg). **B86.1 backlog**: turn 被 drop 后重算时间轴(step345)防字幕/音轨错位(绝不静止固有取舍, B84+双救后极罕见). CCP S3+S4. |

### 👥 团队进化 (2026-05-25 议定 · 共 11h, 5 个 PR)

LLM 角色团队从「各跑各的」升级为「真正协作」+ 专业化分工。

| PR | 工程量 | ROI | 状态 | 备注 |
|---|--------|-----|------|------|
| ~~**PR-1 · 跨层 Context 协作**~~ | ~~3h~~ | ✅ shipped 2026-05-25 | 编剧 _generate_adsd_dialogue_turns 新增 topic_meta + historical_context 参数 · prompt 头部注入「总制片人文化铁律」(CULTURE/REGION/ERA/PERIOD_COSTUME/NEGATIVE) · 编剧台词必须严格匹配上游文化年代 · 1h vs 3h 预算 (节奏 Reviewer 在 B9 已 ship 简化路径) |
| ~~**PR-2 · 题材专家路由**~~ | ~~2h~~ | ✅ shipped 2026-05-25 | DIRECTOR_STYLE_ROUTES 7 种路由表 (intimate_wuxia/imax_war_epic/saturated_folk/slow_poetic/gritty_kinetic/classical_realism/modern_documentary) · _director_route_block helper · topic_decomposition LLM 输出 director_style_route 字段 · jiangwen_prompt 用单一路由风格关键词不再混搭 6 大师 |
| ~~**PR-3 · 专业角色补全** (旧版)~~ | ~~4h~~ | ~~★★★~~ | 🔀 **2026-05-28 拆分为 PR-3a/b/c/d 配 OiiOii 制片中台** | 原计划 4 LLM 角色 (武术/美术/剪辑/音效) 升级为 OiiOii 7-Agent 团队化, 配 creative_brief.json 制片中台架构 (Codex Stage 1+2 共识融合方案) |
| ~~**PR-3a 制片中台 creative_brief.json**~~ | ~~★★★ P0~~ | ~~4h~~ | ✅ shipped 2026-05-28 (CCP 全 4 阶段) | OiiOii 7-Agent 升级地基. 新增 voice_assets/briefs/{topic_safe}.json schema v1 (`adr_creative_brief_v1`). 字段: schema/topic/created_at/updated_at/_brief_version/agents (topic_decomposer/art_director/audio_director/shot_director/character_designer)/brief (metadata/audio/visual/characters/scenes/shot_list/pacing/flags)/history. helper 全套: `_briefs_dir/_brief_path/_empty_brief/_load_brief (atomic + topic 串档校验 + deep_merge 老 schema)/_save_brief atomic rename/_brief_get/_brief_set dotted path/_brief_claim conflict warn allow + history audit/_brief_agent_status (pending/running/succeeded/failed/skipped)/_brief_from_topic_decomposition mapper (Stage 4 agent_status succeeded 跳过幂等)`. `_llm_topic_decomposition` LLM 推断 + cache 命中两路径都自动 mapper 写入 brief, 失败不阻断主流程. Stage 3 codex 抓 3 issue: High (topic 串档 → 校验) / Medium1 (cache 命中也回填) / Medium2 (deep_merge nested 字段). Stage 4 抓 1 (mapper 重复 history 11→22 → guard). 12/12 unit test 全过. 下游待 PR-3b/c/d/e 各自填字段 |
| ~~**PR-3b jiangwen 结构化分镜 schema (简化版)**~~ | ~~★★★ P0~~ | ~~1.5h~~ | ✅ shipped 2026-05-28 (CCP 全 4 阶段) | Codex Stage 1 共识简化: 只到 brief.shot_list 写入, 不动 step6/65 集成 (留 PR-3b.1). jiangwen JSON schema 加 5 enum 字段 (shot_type/camera_angle/lighting/camera_motion/visual_motif) + Python 兜底解析. enum 白名单校验 (_SHOT_TYPE/_CAMERA_ANGLE/_LIGHTING/_CAMERA_MOTION_ENUM 集合 + _validate_enum helper, 非法 → default + fallback flag). _brief_claim shot_director 写 brief.shot_list, status==succeeded 跳过跨 run 防 history 膨胀. Stage 3 codex 抓 3 issue: High (enum 校验) / Medium (missing vs invalid 分开统计) / Low (status guard). 4/4 enum 单测 + 4/4 shot_list 构造单测全过 |
| ~~**B52 grid_multiref TTS-aware duration**~~ | ~~★★★ P0~~ | ~~1.5h~~ | ✅ shipped 2026-05-28 (CCP Stage 1+3, Stage 4 timeout 但 AST+单测全过) | 大哥 224938 反馈"末句截断". root: _grid_multiref_duration 按 sum(scene.vid_duration) 算 = 18s 容器, voice clone 评书 7 句 21-28s 被压. 修: (1) _grid_multiref_duration 加 hint 参数 (total_panels, tts_duration_buffered), 算法 max(scene_sum, tts_buffered × group_panels/total_panels) cap 15 (2) _grid_multiref_tts_buffer_factor env ADR_GRID_MULTIREF_TTS_BUFFER 默认 1.3 评书 buffer + isfinite 防御 (3) _grid_multiref_tts_duration_buffered ffprobe master_voice.mp3 失败 0 backward compat (4) caller 算 _b52_tts_buffered + _b52_total_panels (Stage 3 Medium fix: 排除 <2 尾组) 传给 duration. Stage 3 codex 抓 Medium (total_panels 排尾) + Low2 (env nan isfinite). 例: 7 panel TTS 18s×1.3=23.4s → group1=13s (+30%) group2=10s (+25%) 总 23s. B52.1 长期: voice clone ASR 校验末句完整率 |
| ~~**PR-3b.2 jiangwen 5 enum 强约束 + blueprint fallback**~~ | ~~★★★ P0~~ | ~~1h~~ | ✅ shipped 2026-05-28 (CCP Stage 1+3+4) | 223716 实测 PR-3b shot_list: LLM 漏字段 7/7 — Opus 把 enum 揉进 `prompt` 长文本不单独输出. 修 3 处: (1) 新增 _shot_blueprint_enums(i, n) 按位置硬分配 5 enum dict, 全部值在 _PR3B1_*_ENUM 白名单 (i=0 钩子 close-up + push-in, i=n-1 优先收尾 extreme wide + pull-back, 中段 wide/over-the-shoulder/medium 变化分布) (2) PR-3b shot_list 构造时 bp_enum 始终算当 default per-field (Stage 3 High+Medium fix), LLM 漏字段或非法 enum 都 fallback bp.该字段, blueprint_fallback_count audit (3) jiangwen prompt 末尾输出格式块加 PR-3b.2 强约束 "8 字段必须独立填写, 不允许只塞 prompt 长文本". Stage 3 codex 抓 3 issue: High (bp 始终当 default) / Medium (per-field 检测) / Low (n=2 末镜优先). 4/4 Stage 3 单测全过 |
| ~~**PR-3b.1 step6/65 集成结构化分镜**~~ | ~~★★ P1~~ | ~~1.5h~~ | ✅ shipped 2026-05-28 (CCP Stage 1+3+4, Stage 1 timeout 接受方向) | 选 scene 字段透传路径而非 brief 查询 (scene 是 pipeline 事实源). 3 处改: (1) step1 L4313 script item 加 5 enum + scene_anchor 字段透传自 visuals[i] (2) _storyboard_grid_prompt L7202 panel 行加 'Cinema: Shot/Angle/Light/Cam | Motif' (3) _build_motion_video_prompt HADS 非 ADSD 路径优先 scene.camera_motion + cinema_tail (shot/angle/lighting). Stage 3 codex 抓 Medium (scene 字段透传 raw 未校验 → 新增 module-level _PR3B1_*_ENUM + _validate_enum_field helper 60 char 限长 + newline/tab 注入过滤 + 白名单) + Low2 (static 覆盖 motion_prompt → static 时 fallback motion_prompt 不覆盖). Low1 (action_block 空句点) 接受. 8/8 Stage 3 修复单测全过 |
| ~~**PR-3c 音效师 Agent Phase 1 (设计层)**~~ | ~~★★ P1~~ | ~~1.5h~~ | ✅ shipped 2026-05-28 (CCP 全 4 阶段) | OiiOii 7-Agent 音效师. 简化版只到 brief.audio.sfx 设计层不动 mux (Codex Stage 1 共识同 PR-3a/3b 套路). 新增: _SFX_TYPE_ENUM (17 类) + _SFX_INTENSITY/_POSITION_ENUM + _rule_based_sfx_design(era 古→wind hook / action→metal_clang, mood 悲压→breath / 战激昂→sword, topic 海→sea_waves payoff / modern→crowd_murmur 关键词路由) + _validate_sfx_entry (enum 白名单 + 限长 + NaN/Inf 防御 math.isfinite) + _audio_director_design Tier 2 creative Flash 2.5 LLM 主案 + 规则兜底 + markdown 包装 strip + LLM arr cap 7. step1 末尾 _brief_claim audio_director 写 brief.audio.sfx, status==succeeded 跳过. Stage 3 codex 抓 Medium (反射 → 直接 json.loads) + 2 Low (NaN/Inf + arr cap). 7/7 unit test 全过 |
| **PR-3c.1 step9 mux SFX 三层 amix** | 3-4h | ★★ P1 | planned | PR-3c Phase 2 (留长期). 需 SFX asset 生成路径 (WeryAI BGM 接口能否当 SFX 用未验证 / 外部免费 SFX 库 freesound API key). step9 mux 从现 voice+BGM 两层升 voice+BGM+SFX 三层 amix, trigger_at_sec 精确插入. 配 brief.audio.sfx 已有数据 |
| ~~**PR-3d 美术指导 Agent 规则版 Phase 1**~~ | ~~★★ P1~~ | ~~45min~~ | ✅ shipped 2026-05-29 (CCP 全 4 阶段) | OiiOii 7-Agent 第 4 个. Codex Stage 1 共识规则版 (不调 LLM, 复用 DIRECTOR_STYLE_ROUTES + era 关键词路由). 新增: _GRAIN_LEVEL_ENUM (4 档 clean/light_grain/medium_grain/heavy_grain) + _hex_color_validate (#RRGGBB) + _rule_based_art_design (7 route × 5 hex palette / era 路由 costume + props / route map grain + style_anchor 复用 anchor) + _validate_art_design (palette cap 5/非法滤 + costume 限长 300 + props 空 sub-list 跳过 + grain enum 白名单 + anchor 限长 400). step1 末尾 PR-3c 之后 _brief_claim art_director 5 字段写 brief.visual.*. status==succeeded 跳过. Stage 3 codex 抓 Low1 (空 props 跳过) + Stage 4 抓 historical era props 关键词漏 (加 'historical' 跟 costume 对齐). 6/6 unit test + 3/3 Stage 3 + 1/1 Stage 4 修复全过 |
| **PR-3d.1 storyboard_grid/character_sheet prompt 注入 + cultural_qa palette 校验** | 2-3h | ★ P2 | planned | PR-3d Phase 2 (长期). 把 brief.visual.style_anchor 注入 _storyboard_grid_prompt 头部 + brief.visual.costume 注入 character_sheet prompt + cultural_visual_qa 加 palette hex 校验 (生成图片用 ASR/Image API 提取颜色对比). 让 PR-3d 设计真正影响图片 |
| ~~**PR-3e Art Director Coordinator 规则版 Phase 1**~~ | ~~★★ P1~~ | ~~1h~~ | ✅ shipped 2026-05-29 (CCP Stage 1+3+4, Stage 1 timeout 接受方向) | OiiOii 7-Agent 第 5 个 (终极锁版 coordinator). 规则版 (Codex 共识跟 PR-3c/3d 同套路, 不调 LLM). 新增: _COORDINATOR_VERSION=1 + _coordinator_review(brief) 5 类规则冲突 (era_costume_mismatch / shot_list_monotone / sfx_distribution_incomplete / palette_missing_or_incomplete / agents_not_all_succeeded). 输出 brief.coordinator (conflicts list / decisions [] / locked=True / locked_at / version / conflict_count). step1 末尾 PR-3d 之后调 _brief_claim coordinator. _empty_brief schema 加 agents.coordinator + brief.coordinator 默认空骨架. Stage 3 codex 抓 Medium2 (era token 边界 re.split 防 shanghai 假古风) + Low (shot_list valid_shots 数量 check). Stage 4 抓 palette hex 校验. 6/6 + 4/4 + 2/2 unit test 全过. 武术/剪辑/character_designer 留长期 (现 B6/silent_b/B31 已部分实现) |
| **PR-3e.1 LLM 主案 + 自动 fix + 下游集成** | 3-5h | ★ P2 长期 | planned | PR-3e Phase 2. LLM Tier 1 Opus 看整 brief 判智能冲突 (vs 规则版固定模式). 自动 fix 冲突 (e.g. era 古风时强制更新 costume). 跟下游集成 (step6/65 读 brief.coordinator.locked, 否则阻断生成). 武术指导 / 剪辑师 单独 agent 也合入 |
| ~~**PR-4 · LLM 分级**~~ | ~~1h~~ | ✅ shipped 2026-05-27 (CCP 全 4 阶段, 30min 简化) | L1647 `_LLM_TIER` 字典 4 档 (producer Opus / creative G25 / review GLite / data Haiku) + `tier_chat` helper, `chat()` 旧签名 100% 兼容 26 调用点零侵入 · 白名单降级 3 处 Tier 4 短任务 (L3228 改写 topic 200tok / L8076 单 voice_id 80tok / L15684 黄历注脚 200tok) · Stage 1 codex 推 30min 简化 / Stage 3 抓 Medium (注脚未传 max_tokens 走默认 4096 → 修补) · 分布: Opus 1 / G25 9 / GLite 6 / Haiku 3 (3/12 G25 降级到 Haiku, 实际省成本约 7% 不是预期 30% — 单点保守降级) |
| **PR-5 · 团队学习机制** | 与达尔文联动 | ★★ | planned | 每个角色记录 historical_success_rate 输出是否最终通过 audit · 失败 case 反馈对应角色 prompt · 团队自动进化适应 ADR 项目 |

### 🧬 达尔文进化 (2026-05-25 议定 · 共 11.5h, 分 4 个 Round)

让 ADR 自动迭代优化：每次 run 收集质量数据 → 失败模式自动反哺 prompt → 高分配置自动遗传。

| 阶段 | 工程量 | ROI | 状态 | 备注 |
|------|--------|-----|------|------|
| ~~quality_audit.py 适应度评估器~~ | ~~1h~~ | ~~★★★~~ | ✅ shipped 2026-05-25 | 扫所有 run 输出 通过率/SFX/fallback/audit msg + markdown 趋势报告 |
| ~~**阶段 A · Fitness Scorer 强化**~~ | ~~1.5h~~ | ✅ shipped 2026-05-25 | quality_audit 加权 (50% pass + 15% action + 15% retry 效率 + 20% audit) · band 分级 elite/strong/viable/weak/failed · --threshold 过滤 · Top 5 Elite 排行 |
| ~~**阶段 B · Audit 黑名单自动学习**~~ | ~~2h~~ | ✅ shipped 2026-05-25 | tools/learn_audit_blacklist.py 扫所有失败证据 LLM 提炼 11 触发词 · audit_blacklist.json · script-gen prompt 用「类别+数量」抽象描述注入 (避免原词二次触发审核) · codex 审查通过 |
| ~~**阶段 C · 题材分类器**~~ | ~~1h~~ | ✅ shipped 2026-05-25 | topic_decomposition LLM 加 audit_risk_score (0-100) + audit_risk_reason · script-gen risk_strategy_block 按 >=61/31-60/<30 三档脱敏 · cache schema v4 自动失效旧 |
| ~~**阶段 E · 自动 IP 进化**~~ | ~~2h~~ | ✅ shipped 2026-05-25 | tools/evolve_ips.py 扫历史 run 算 IP fitness · <60% 自动重孵化 (删 grid+清 cache+标 needs_reincubate) · >=90% production_ready · 6 attempt 阈值防新 IP 误判 · 实测: 科比 0% reincubate / 唐僧/孙悟空/旁白/曹操/紫霞 production_ready |
| ~~**阶段 C+ · 高危题材 LLM 改写**~~ | ~~1h~~ | 🟡 shipped 2026-05-26 (default-off) | _maybe_neutralize_topic LLM 改写高危题材替换名人/品牌 · 实测科比 7/12 pass + 1 SFX 通过但视频 5 cover 兜底不能看 · 改 opt-in: ADR_NEUTRALIZE_HIGH_RISK_TOPIC=1 显式启用 |
| ~~**阶段 C++ · Hard+Smart Abort**~~ | ~~30min~~ | ✅ shipped 2026-05-26 | A: step1 检 audit_risk>=75 直接 raise + TG 提示改题材 (省 30min credits) · B: step66 A-roll fail>=50% 或 total fail>=60% 自动 kill · _parse_risk_score helper 防 NaN/Infinity 抓最大数字 + 关键词优先 · codex 审查通过 |
| ~~**R3 · 题材推荐器**~~ | ~~1h~~ | ✅ shipped 2026-05-26 | tools/topic_recommender.py 扫历史 fitness 数据 · sliding window n-gram + Jaccard 聚类 + confidence 标记 · 自动识别 elite (>=80) 题材模式 + failed (<30) 模式 · --next 推下一个题材 · codex 审 5 fix |
| ~~**R4 · Batch 演化 Runner**~~ | ~~1h~~ | ✅ shipped 2026-05-26 | tools/evolve_runner.py 闭环达尔文 · 读 R3 elite 关键词 + LLM 生成 N 个新题材(变异) · 自动跑 ADR(选择) · 跑完自动 evolve_ips + learn_audit_blacklist(遗传) · argparse + --max-hours 全局 budget · _validate_topics 验证 LLM 输出 · 实测生成「蟠桃偷渡客紫霞泪未干」继承 elite 气质 |
| ~~**R5 · 进化世代追踪**~~ | ~~30min~~ | ✅ shipped 2026-05-26 | tools/evolution_log.py 世代曲线观测器 · evolution_history.json 持久 ship_log · 按日期 generation 分组 · run-weighted trend 不偏 sparse 日子 · 实测: 前期 31.4 → 后期 68.5 (**+37.1** 正向进化 ✓) · codex 3 fix 全应用 |
| **阶段 D · Multi-arm Bandit prompt** | 4h | ★★ | planned (Round 3) | 每个 prompt 函数维护 3 个变体 · epsilon-greedy 选 (90% best, 10% explore) · variant 表现存数据库 · 月度 review 删差 variant |
| **阶段 F · Batch 自动跑 daemon** | 1h | ★ | planned (Round 4) | tools/evolve_run.py daemon · 每天 03:00 自动跑 3-5 个题材 · 跑完自动 audit + 黑名单更新 + IP 重孵化 · TG 推送报告 |

### 🐛 2026-05-21 笑傲江湖重跑发现的 bug

| Bug | 严重度 | 工程量 | 状态 | 现象 + 修复方向 |
|-----|--------|--------|------|----------------|
| ~~**B1 音量太低**~~ | ~~★★★ P0~~ | ~~1h~~ | ✅ shipped 2026-05-21 | step9 amix 默认归一化压人声 → normalize=0 + weights 显式权重 + voice 1.0→2.0 + BGM 0.85→0.4，两阶调音通过 |
| ~~**B2 meta_grid 12 宫格漏进**~~ | ~~★★★ P0~~ | ~~1.5h~~ | ✅ shipped 2026-05-21 | step7 防御层缺失 seg 时用 meta_grid 当 still 导致 4×3 grid 漏入画面 → 检测 meta_grid_ 文件名 + 优先用邻 turn 合法图/cover.jpg 兜底 + prompt 强化禁止复刻 grid + negative_prompt 加 grid/split/panel ban |
| ~~**B3 内嵌字幕**~~ | ~~★★★ P0~~ | ~~30min~~ | ✅ shipped 2026-05-21 | meta_grid_call prompt 强化禁止复刻参考图中文标签 + negative_prompt 强化 text ban，重跑笑傲江湖未复现 |
| **B7 名人题材 Image audit fail** | ★★★ P0 | ? | **blocked** | 2026-05-22 诊断：科比/NBA 题材 GPT_IMAGE_2 画真人+商标 → WeryAI 平台 Image asset audit 拦截全部 task。DOUBAO spike 验证：跨端点 fallback 无效（同 audit 服务）。**唯一路径：避免 GPT_IMAGE_2 画名人脸+商标**（重写 step6 prompt 抽象化 / 直接放弃现代名人题材）|
| ~~**B8 历史顾问条件触发**~~ | ~~★★ P1~~ | ~~30min~~ | ✅ shipped 2026-05-26 (CCP) | 新增 `_should_run_immersion_qa(topic)` gate 看 topic_decomposition.era · historical_/period_/ancient_/classical_ 前缀或含 '古'/'history' → 跑 LLM 审稿; 现代/商业/科技 → skip 防误替换 (云计算工程师→画师 这种荒谬) · env ADR_IMMERSION_QA_ALL=1 强制 override · 5/5 case 测试 (西游/紫霞跑, 科比/南京/云计算跳过) · CCP 全 4 阶段走完 |
| ~~**B9 B-roll 出戏**~~ | ~~★★★ P1~~ | ~~2.5h~~ | ✅ shipped 2026-05-25 | A: _broll_rhythm_reviewer LLM 节奏审稿 (keep/merge/rewrite/relocate 决策) · B: script-gen prompt 加 silent_b/narrated_b shot 必须继承前 turn 视觉元素铁律 · 同时收紧 max_narrated 3→2 / silent_b 占比 20-35%→15-25% · env ADR_BROLL_REVIEWER 控制 |
| ~~**B10 ADS 单旁白多音色**~~ | ~~★★★ P1~~ | ~~1h~~ | ✅ shipped 2026-05-26 | **现象**: ADS (HADS/VADS) 单旁白模式跨 scene 音色不一致 · **Root cause**: step65 motion audio_dub `_select_voice_asset_reference` per-scene 走 gender fallback · **修**: 新增 `_PODCAST_TO_VOICE_ASSET_MAP` 映射 podcast_id → voice_asset_id · `_podcast_id_to_voice_asset` helper · step1_script 末尾把映射后 voice_asset 写入所有 script[i]["voice_asset_id"] · 复用现有 line 12527 优先级 (scene.voice_asset_id 高于 gender fallback) · 阶段 3 (跨 scene task 波动) 留 PR-A 合并跑根治 |
| ~~**B11 meta_grid 竖屏适配**~~ | ~~★★★ P1~~ | ~~30min~~ | ✅ shipped 2026-05-26 (CCP) | 李莫愁 VADS 反馈: meta_grid 硬编码 16:9 不适配 9:16 视频 · 修: `generate_character_meta_grid_gpt_image2` 按 IS_VERTICAL 切, 竖屏 3 cols × 4 rows + 9:16(4k) + vertical portrait 框, 横屏保持 4×3 + 16:9 · `_meta_grid_panel_index` 加 VDAR 分支 (col 0-2 mod 3 防 C3 漏定位, row 0=wardrobe/1=action/2=close-up) · `_character_meta_grid_cache_path` 加 _h/_v suffix 防 cross-aspect 污染 · 20/20 case 全过 · CCP 全 4 阶段 |
| ~~**B12 h/v 默认 motion (HADS/VADS)**~~ | ~~★★★ P1~~ | ~~10min~~ | ✅ shipped 2026-05-26 (CCP, 二次修) | 用户认知: 跑 ADR 默认就是 HADS/VADS (动态), 不是 HADR/VADR (静态) · 修: WITH_MOTION = not ADSD and not --no-motion · 7/7 矩阵: HADS/VADS 默认 ON · HADSD/VADSD 走 step66 不动 · HADR/VADR 需 --no-motion 显式 · 第一版只放开 VDAR 被大哥纠正, 二版 h/v 同步 |
| ~~**B12.1 删除冗余 --with-motion**~~ | ~~★★ P2~~ | ~~10min~~ | ✅ shipped 2026-05-26 (CCP) | B12 后 --with-motion 与默认行为重叠成 dead flag (大哥: 脱裤子放屁) · 修: run_adr_v8.py L158 删 flag 检查, README/INDEX/skill 全部去 --with-motion 用法, 改用 --no-motion 退化 · 老脚本传 --with-motion 自然 no-op (无 argparse 不报错) · 6/6 矩阵通过 |
| ~~**B12.2 fmt_label 命名对齐**~~ | ~~★ P3~~ | ~~15min~~ | ✅ shipped 2026-05-26 (CCP) | 李莫愁 VADS 端到端验证通过但成片仍叫 ADR_V8_VDAR_*.mp4 (旧 fmt_label 残留) · 修: 新增 CURRENT_MODE_LABEL/CURRENT_MODE_WITH_ASPECT 模块级常量 (L961-970) 按 ADSD/WITH_MOTION/IS_VERTICAL 三元决策 · 替换 6 处旧 VDAR/HDAR (fmt_label/motion_qa mode×2/fmt_tag/execution_plan) · README/skill 同步 · 6/6 矩阵: VADS/HADS/VADSD/HADSD/VADR/HADR · 文件名从此为 ADR_V8_VADS_*.mp4 |
| ~~**CCP v1.0 协同协议**~~ | ~~1.5h~~ | ✅ shipped 2026-05-26 | tools/codex_collab.py 4 阶段 CLI (review-spec/propose-alt/review-code/final-qa) · voice_assets/collab_history.json 跨 session 持久 · CLAUDE.md ADR-CCP 强制规则 · feedback memory feedback_adr_ccp_protocol · 让任何 Claude session 自动走协同, 不再单向 ship 后才 codex 审 |
| ~~**B10.1 mapping 女声修订 + 防回归**~~ | ~~30min~~ | ✅ shipped 2026-05-26 (CCP 首跑) | 李莫愁思春 VADS 实测发现 BY2 是「甜妹歌声 music_contaminated」克隆不稳 · **修**: 女声映射 BY2 → 蒙曼/张泉灵/武亦姝 (说话类) · 加 `_VOICE_ASSET_FORBIDDEN_FLAGS` (singing_not_speech/music_contaminated/not_vocal_separated) + `_voice_asset_is_speech_safe` helper 自动过滤 · CCP 全 4 阶段走完: codex 提融合方案 A+B / Stage 3 自审发现 not_speaker_diarized 误加移除 / Stage 4 PASS · 协同验证 |
| ~~**B4 武戏无 SFX 配音**~~ | ~~★★ P1~~ | ~~2h~~ | ✅ shipped 2026-05-23 | action_b generate_audio=true + SFX_DIRECTIVE prompt · 西游记 turn 4 验证通过 |
| ~~**B5 LLM 把场景描述当 speaker**~~ | ~~★★ P1~~ | ~~1h~~ | ✅ shipped 2026-05-21 | prompt 加 speaker 铁律 + _sweep_speaker_field 后处理 (bad_keywords 检测 + 从 bad speaker 提取已知角色 + role_candidates fallback) · 7/7 case 通过 |
| ~~**B6 短喊招漏关键词**~~ | ~~★ P2~~ | ~~15min~~ | ✅ shipped 2026-05-21 | _is_action_shout 加 再来/换我/还击/反攻/拦住/挡住/拼了/决胜 等 16 个攻势/反击/换场词 · 6/6 case 通过 |
| ~~**B13 silent_b 不该计入 ADSD speaker_count**~~ | ~~★★ P1~~ | ~~30min~~ | ✅ shipped 2026-05-27 (CCP) | 「南京学区房」HADSD speaker_count=5 (silent_b 被算第 5) 触发 delivery fail · 修 2 处: (1) `_llm_assign_voice_assets` L601 turn_summary 过滤 silent_b + isinstance dict guard, 空时早出 (2) `delivery_qa` L14685 speakers 过滤 silent_b + (silent) + 空字符串, dict.fromkeys 去重 · 5/5 case: 南京 5→4 / monologue 1 / dialogue 2 / non-dict / 空 timeline · CCP Stage 3 抓 Medium (isinstance guard) + Stage 4 抓 ROADMAP/ship_log 未同步 全补 |
| ~~**B14 storyboard prompt 缺台词主题锚定**~~ | ~~★★★ P1~~ | ~~45min~~ | ✅ shipped 2026-05-27 (CCP 全 4 阶段, 融合方案 C) | 「南京学区房」12/10 text_visual_alignment fail · 修 3 处: (1) L6210 抽 `_extract_core_terms` helper + `_CORE_TERMS_STOP_WORDS` 常量, QA 复用 (2) L3667 lines_display 后预抽 `_core_terms_per_line`, jiangwen_prompt 注入 "B14 台词主题锚定" 段 + 显式约束 LLM 每句 verbatim 含 1-2 个 core term (按 core 数动态对齐 QA 阈值) (3) L3899 visuals 落地后 Python append 兜底, 缺词 scene 末尾追加 `, scene depicting spoken elements verbatim: X, Y, Z` (top 3 missing) · Stage 1 codex 推 30min 简化 / Stage 2 给方案 B 结构化锚点 / Stage 3 抓 High (描述矛盾→改) + Medium (token 粗切→留 B14.1) + Low (武戏 panel→接受) · 3/3 mock 通过 |
| **B14.1 core_terms token 抽取改 jieba 分词** | ★ P3 | 1h | pending | B14 现复用 QA 同款 2-6 字贪婪 regex 抽中文片段, 容易抽出 "在南京/一套房子不仅/是居住空间" 这种伪关键词 (句法断片不是名词) · 现状: append 兜底虽能命中 QA 阈值但 prompt 看起来生硬 · **修方向**: `_extract_core_terms` 改用 jieba 分词 + POS 过滤 (只取名词/地名/专名), QA 同款抽取也跟着升级 · 工程量包含: jieba install/lazy import + stop_words 扩展 + 单测对齐 (避免 QA 阈值意外变化) |
| ~~**PR-A.1 step66 _batch_groups 检测**~~ | ~~★★ P2~~ | ~~1h~~ | ✅ verified 2026-05-27 (override fixture 实测) | 用 /tmp/adr_script_override.txt 强制 fixture 注入 3 个 multi-turn group · debug log 完整显示: `_batch_groups final=[[1,2],[6,7]]` + tg "🚀 PR-A merged_a 启用" + 2 group future + 6 scene future submit + fallback 路径全 work · group_1_2 fail (rate limit 1001) + group_6_7 fail (image audit B7), 都 fallback 逐 turn 路径正常 · 上次"罗永浩 v1 _batch_groups 空"推测是 dashboard cache 残留 + monitor filter 错读其他 run msg · PR-A 框架本体 100% 验证 work, runtime hidden bug 不存在 |
| **B19 override script (silent) regex None bug** | ★ P3 | 5min | ✅ shipped 2026-05-27 | `_parse_adsd_override_turns` L2239 `silent_match.group(1).strip()` 在 capture 缺省时 None crash · 修: `(silent_match.group(1) if silent_match else None) or ""` 兜底 |
| **B20 needs_lip_sync=None default 应 True** | ★ P3 | 15min | pending | override 注入路径未设 needs_lip_sync, dict.get 返回 None 而不是 True default · PR-A all_a_roll check `script[i].get("needs_lip_sync", True)` 在 key 存在但 value=None 时返回 None ≠ True → group reject · **修方向**: PR-A check 用 `script[i].get("needs_lip_sync") is not False` 让 None/True/缺省 都算 a_roll; 或 override parser 显式赋 needs_lip_sync=True 给非 silent turn |
| **PR-A.2 rate limit burst submit** | ★ P3 | 30min | pending | PR-A 启用时 2 group + 6 scene 共 8 submit 在短时间内 burst, 撞 WeryAI 1001 频限 (2.5s 节流不够) · **修方向**: group submit 给单独节流 slot (5s 间隔), 或 group submit 走专用慢队列让 scene 优先 |
| ~~**B21 删 Smart Abort Ken Burns 兜底 opt-in**~~ | ~~5min~~ | ✅ shipped 2026-05-27 | 大哥指: Ken Burns 兜底没意义, 别留绕过口子 · 修 L13893: 删 ADR_SMART_ABORT_HIGH_FAIL env override (abort 永远生效) · 删 err msg "或显式 opt-in env=0 强制出 Ken Burns 兜底片" 提示 · 措辞 "成片大半 Ken Burns 兜底" → "成片大半失败" · Hard Abort (L3180) 仍保留 env opt-out 不动 (那是改 topic 路径) |
| **B22 audit_risk_score 校准 (65→75)** | ★ P3 | 30min | pending | 罗永浩题材 audit_risk_score=65 < 75 没触发 Hard Abort, 但实测 step66 A-roll 4/6 fail 触发 Smart Abort · 65 评分偏低 · **修方向**: 跑 evolve_ips 用 Smart Abort 历史数据回写 audit_risk; 或 LLM topic_decomposition prompt 加 "现代真人 + 品牌组合 → 75+" 校准规则 |
| ~~**B56 ADSD IP voice 优先级 (扩 B50 到 ADSD)**~~ | ~~★★ P1~~ | ~~30min~~ | ✅ shipped 2026-05-29 (CCP Stage 1+3) | 120745 实测 ADSD 走 LLM 分配忽略 IP voice. 修 3 处: (1) 新增 _ip_voice_asset_for_speaker(speaker) helper (2) _finalize_adsd_turns L2378 加前置 IP voice check (3) _apply_llm_voice_assignment L734 加 IP-locked 跳过 |
| ~~**B56.1 IP voice 强制覆盖 LLM 选择 (122716 实测 B56 没生效 fix)**~~ | ~~★★★ P0~~ | ~~10min~~ | ✅ shipped 2026-05-29 |
| ~~**B60 voice clone 情感引导**~~ | ~~★★ P1~~ | ~~45min~~ | ✅ shipped 2026-05-29 (CCP Stage 1+4) | 大哥 154014 反馈"音色一点感情都没有". 修 3 处: (1) module-level _EMOTION_NARRATION_STYLE_MAP (19 emotion → 11 unique style 短句: urgent tense/restrained somber/solemn gravitas/introspective hushed/passionate intense/reflective wistful/warm intimate/bright energetic/uplifting warm/playful curious/elevated surprised) + _EMOTION_DEFAULT_STYLE + _voice_clone_emotion_style 收集 cap 3 去重 (2) _grid_multiref_prompt B49 narration_block 加 'Voice style: {emotion_style}. ... do not flatten into neutral newsreader delivery' (3) _lip_sync_one_group PR-A merged_a B55 voice_clone 分支 同样加. 6/6 unit test 全过. _motion_audio_dub_prompt per-scene 已有 strict_voice 参数未改 (留 B60.1 长期) |
| **B60.1 per-scene motion_audio_dub_prompt emotion 注入 (长期)** | ★ P3 | 30min | planned | B60 grid_multiref + PR-A merged_a 已 ship. per-scene _motion_audio_dub_prompt 已有 strict_voice 但缺 emotion style 引导 (跟 grid_multiref 不对称). 等 grid_multiref / PR-A 主路径稳定后补 per-scene fallback 路径 |
| ~~**B61 voice clone 字幕对齐 (简化版 A)**~~ | ~~★★★ P0~~ | ~~30min~~ | ✅ shipped 2026-05-29 (CCP Stage 1+4) | 154014 反馈"旁白字幕对不上". Codex Stage 1 共识简化方案 A: step8 入口 ffprobe raw_concat.mp4 audio + master_voice.mp3 算 scale = clone/tts, 0.5<scale<3.0 + abs(scale-1)>0.05 容差才缩放, 全 script[i].sub_start/audio_start/vid_duration × scale. 防重复缩放 (_b61_subtitle_scaled flag). Stage 4 抓半缩放 → per-turn isolation: 单 turn 内全字段校验完再原子更新, 异常 turn 整 turn 跳过. 7 case test verify. 不影响主流程 try/except 兜底. B61.1 长期: faster_whisper ASR force align word-level 重写字幕 (95% 准确率 vs 简化版 80%) |
| **B61.1 voice clone ASR force align (长期)** | ★ P2 | 2-3h | planned | B61 简化版均匀缩放未覆盖不均匀 turn 偏移. 完整方案: faster_whisper ASR raw_concat audio → segment-level timestamps → fuzzy match script[i].text (Chinese char overlap) → 重写 sub_start/audio_start/vid_duration. 等 B61 实测验证后再决定是否升级 |
| ~~**B72 voice clone 字幕尾部缺失 (B61/B61.1 漏改 sub_end)**~~ | ~~★★★ P1~~ | ~~~30min~~ | ✅ shipped 2026-05-31 (CCP Stage 1+3+4, Stage 2 跳过-5行内例外) | **根因(4-agent投查+代码确认)**: B61.1 ASR对齐(L16521)和B61均匀缩放(L16561)都只重写 sub_start/audio_start/vid_duration, **漏 sub_end** → step8 写ASS字幕End被 cap_end=s['sub_end'] 封顶在TTS时长(57.5s), 而clone旁白75.9s. **修(2处)**: B61.1 加 `s['sub_end']=max(ns,ne)`+isfinite守卫(Codex Med); B61 字段元组加'sub_end'同scale. **真实验证**: chunk1 重跑 step8 末turn sub_end 57.53→73.80s, 重烧录(step9)修正版 t=65s 帧现显字幕"爽籁发而清风生纤歌凝而白云遏"(修复前空白). py_compile PASS. 配套 redeliver: 5段 reburn(step8+step9 复用motion)→concat→推修正版长片. **影响所有 voice clone run**(非B69锅, B69长片曝出). 原现象: 滕王阁序 B69 E2E 实测(大哥反馈"字幕不全") **每 chunk 末 ~15-18s voice clone 旁白在念但无字幕**. root: 字幕时间轴按 TTS 算(chunk1 句12 结束 57.5s), 实际 clone 音频 75.9s(评书慢 ~1.31×), 字幕提前 ~31% 用完. B61.1(今日 ship)日志显示"ASR force align 字幕重写 12 turn"但 subs.ass 仍止于 57.5s → **B61.1 ASR 对齐实测未把字幕拉伸到 clone 时长**. silencedetect 证实尾部 58-73s 连续旁白非静音. 5 段视频每帧抽样确认: 字幕存在但每段后 1/4 缺失, 全片 ~30% 旁白无字幕. 影响所有 voice clone 单 run(非 B69/long_video 的锅, B69 长片只是放大). 修方向: 查 B61.1 `_b61_1_asr_turn_boundaries` 为何末段未拉伸 — (a) ASR align 的是否非最终 clone 版 raw_concat (b) 只重写了 sub_start 没改 subtitle end/duration (c) n_valid_turns 不匹配走了 fallback 均匀 scale 但 scale 算错. 配 CCP 4 阶段 |
| ~~**B62.1 grid_multiref 末组合并法**~~ | ~~★★★ P0~~ | ~~30min~~ | ✅ shipped 2026-05-29 (CCP Stage 1+3+4 timeout) | 大哥 174943 反馈"节奏改善但还有点赶". root: B62 ceil 法 9 panel × final=4 仍 group_size=3 → 3 group 45s 不够 53s buffer. 修 3 处: (1) _grid_multiref_adaptive_group_size 改 floor 法 group_size = max(2, n // final_n_groups) (2) _generate_grid_multiref_motion_segments loop 改 _b62_1_ranges 列表: 算 ranges 一次, 末组 <2 时删末组 + 前组 end 延伸到 len(refs) 末组合并 (3) _b52_total_panels = len(refs) 末组合并后所有 panel 都进 group. 6/6 unit test 全过: 观沧海 7p→2 group 30s, 满江红 8p→4 group 60s, **将进酒 9p→4 group 60s 容纳 53s buffer ✓**, 长片 22p→7 group 105s, 13p→6 group 90s, 短片 3p→1 group. 容器从 45s→60s (+33%) |
| **B63 jiangwen 限制 panel 数 (短诗 6-8 panel)** | ★ P3 | 1h | planned | B62.1 后续替代方案: 让 jiangwen 短诗题材 (李白将进酒/岳飞满江红等) 强制 6-8 panel 而非 9. 8 panel × group_size=2 末组合并 = 4 group × 15s = 60s 容器自然成立. 美术分镜密度降, 节奏延长 |
| ~~**B62 grid_multiref group_size 自适应**~~ | ~~★★★ P0~~ | ~~45min~~ | ✅ shipped 2026-05-29 (CCP Stage 1+4) | 大哥 170514 反馈"语速太快赶进度". 修: 新增 _grid_multiref_adaptive_group_size(n_panels, tts_buffered, max_per_group=15) helper, 算法 optimal_n_groups=ceil(tts_buffered/15), max_n_groups=n_panels//2, group_size=ceil(n_panels/final_n_groups), clamp [2,12]. _generate_grid_multiref_motion_segments 用 _b62_group_size 替代固定 group_size 当 qa.group_size + loop range. env ADR_GRID_MULTIREF_ADAPTIVE_GROUP=0 opt-out. Stage 4 抓 13 panel × TTS 90s 末组 singleton skip → while loop 减 final_n_groups 直到末组 ≥ 2 (合并末组). 10/10 unit test |
| ~~**B59 use_embedded mux voice 1.8→2.5**~~ | ~~★★ P1~~ | ~~15min~~ | ✅ shipped 2026-05-29 (CCP Stage 1+4) | 大哥 154014 反馈"声音太小". L17102 use_embedded 路径 voice volume hardcode 1.8 → env ADR_USE_EMBEDDED_VOICE_VOLUME default 2.5 (clamp 1.0-4.0 + math.isfinite 防 nan/inf + ValueError 回 default). 跟 master_voice 路径 L17123 voice volume=2.0 对齐 + 易中天/许知远候选低沉音色补偿. log audit. AST PASS, env 5 case 全过 |
| ~~**B66 长文分段渲染 MVP**~~ | ~~★★★ P1~~ | ~~45min~~ | ✅ shipped 2026-05-30 (CCP Stage 1+4) | 大哥 233045 反馈"不是滕王阁序全文". 新增 2 工具: (1) tools/long_text_split.py split_long_text(text, chunk_size=9) 按中文句末标点 split + Stage 4 末尾无标点尾句补 (2) tools/long_text_runner.py 接 topic + text_file, 循环跑 run_adr_v8.py 各段, 收集 final mp4, ffmpeg concat 拼成长视频. 失败段 skip 不阻断. timeout 1h/段. 不动主流. 适用滕王阁序/赤壁赋/醉翁亭记等 800-3000 字古文 | 大哥 233045 反馈"不是滕王阁序全文, 内容不全, 时长不够". root: 滕王阁序全文 ~770 字 35-40 句, ADR fixture/jiangwen LLM cap 9 enum panel (prompt 2000 字限). 单 run 最大 9 句, 无法跑完长文. 修方向: (1) 长文 split 工具 tools/long_text_split.py 把全文拆 N 段每段 9 句 (2) ADR run 改成多段 batch (各段单独 run, 复用 cache) (3) step10 合并 N 个 final mp4 成长视频 (4) 字幕跨段连贯 (重新算 audio_start). 适用滕王阁序/赤壁赋/醉翁亭记等 800-3000 字古文 |
| ~~**B61.1 voice clone ASR force align**~~ | ~~★★★ P0~~ | ~~45min~~ | ✅ shipped 2026-05-30 (CCP Stage 1+3, Stage 4 timeout) | 大哥 233045 反馈"后半字幕已消失但旁白还在念". root: B61 简化版均匀 scale ×1.31 假设各 turn 等比例拉伸, 实测 voice clone 不均匀. 修: 新增 _b61_1_asr_turn_boundaries faster_whisper Chinese segment-level transcribe → 按时间顺序映射 n_turns. step8_subtitles 入口优先 ASR force align, n_valid_turns 严格匹配才 per-turn 重写 sub_start/audio_start/vid_duration, _b61_1_asr_aligned audit. n_seg < n_turns/2 fallback B61 均匀 scale. 3 case mock 映射全过 (1对1/合并/差段) | 大哥 233045 反馈"字幕和旁白不匹配, 常念旁白还在念字幕已消失". root: B61 简化版均匀 scale (×1.310) 假设各 turn 等比例拉伸, 但 voice clone 实际**不均匀** — 某 turn 慢 50%, 某 turn 快 10%, 累计误差让后半段字幕远早于 voice. 修方向: (1) faster_whisper ASR raw_concat.mp4 → segment-level timestamps (start/end/text) (2) Chinese char overlap fuzzy match script[i].text → 重写 sub_start/audio_start/vid_duration per-turn (3) 比 B61 简化版 ×1.310 准确 95% vs 60% |
| **B65 7-Agent 跟现有 LLM 角色融合 (消重合)** | ★★★ P1 | 4-6h | 🟢 **读契约融合 shipped 2026-06-03 (B65.0-3, CCP); B65.4 coordinator-lock + fallback 退役 = 后续 cleanup** | **100% 纯 ADR 无 bot 改动**. **已 ship (2026-06-03)**: B65.0 `_brief_field` read-through 地基 + 3 域消费者迁到 brief 单一契约(各 env-gated 默认关 + 永远 fallback=decomp 原值, brief 由 mapper 镜像同值故 on/off 一致零回归): **B65.1** `_generate_bgm` bgm_style←`brief.audio.bgm_style`(ADR_BRIEF_READ_AUDIO) · **B65.2** director_route←`brief.visual.director_route`(ADR_BRIEF_READ_VISUAL) · **B65.3** `_adsd_role_candidates`←`brief.characters.role_candidates`(ADR_BRIEF_READ_CHARACTERS). cover_art_direction 无消费者跳过. **消双写核心机制已落地**(brief 成读契约). 剩 **B65.4**(coordinator.locked 门控生成 + 总闸 ADR_BRIEF_AS_CONTRACT + 退役 per-domain fallback)= 后续 cleanup PR. **100% 纯 ADR 无 bot 改动**. recon: 双写根源 = `_llm_topic_decomposition` 既返回 9 处直读的 dict, 又 `_brief_from_topic_decomposition` 写 brief 镜像(pipeline 从不回读)→ 70% 重合. 真融合 = brief 当**单一读契约**. **B65.0 已 ship**: `_brief_field(topic,path,fallback)` read-through 访问器(读 brief 单字段, 缺/None→fallback, 永不静默丢数据), 零行为变化无人调用. **B65.1-4 (各自 env-gated 默认关 + 永远 fallback)**: B65.1 `_generate_bgm` 读 `brief.audio.bgm_style`(叶子最安全先验证) → B65.2 视觉消费者(director route/cover) → B65.3 character/role + ensemble cap → B65.4 coordinator lock + 总闸 `ADR_BRIEF_AS_CONTRACT` + 退役 per-domain fallback(单独 cleanup PR). 风险: brief 字段 None 丢数据(_brief_field 强制带 fallback 解)/stale brief(已有 topic== 校验). 原: 大哥架构洞察: 7-Agent 跟 ADR 现有 LLM 角色重合 70%+ (topic_decomposer↔_llm_topic_decomposition / shot_director↔jiangwen / audio_director↔generate_bgm+SFX_DIRECTIVE / art_director↔_director_route_block+director_style / coordinator↔delivery_qa+cultural_qa / character_designer↔speaker_ip / editor↔_broll_rhythm_reviewer+silent_b). PR-3a-e Phase 1 简化 = 双写 (老路径 + brief 镜像), 没真正融合. 修方向 (方案 A OiiOii 风): 老 LLM 角色保留逻辑 + brief 当统一数据契约/audit. 升级 4 处: (1) audio_director 复用 generate_bgm 输出, SFX_DIRECTIVE per-scene 写 brief.audio.sfx (省 1 LLM 调用) (2) art_director 读 _director_route_block.palette 真实文字 → 解析 hex 而非硬编码 (3) coordinator 跟 delivery_qa+cultural_qa 联动, QA fail 时 brief.coordinator.locked=False 阻断 (4) character_designer 把 speaker IP → brief.characters.speaker_ips[] / editor 写 brief.pacing.cuts 从 silent_b 决策 |
| **B64 ADR mode_decider LLM (吸收 bot 层 mode 决策)** | ★★★ P1 | 2-3h | 🟡 **B64.1 code shipped 2026-06-03 (CCP S3+S4, 默认关); B64.2 待大哥 sign-off** | **recon 修正**: bot 其实**没硬编码关键词路由** —— mode 决策是 bot 的 Claude 在 tool-call 时按 `registry.py` schema(L138-192)+ `persona/SOUL.md`(L121-230)的自然语言规则做的(LLM tool-use). **B64.1 已实现 (ADR 侧, env `ADR_MODE_DECIDER` 默认关零回归)**: `_llm_topic_decomposition` 加 `mode`(ads/adsd/adr)字段 + cache v4→5 + `_apply_llm_mode_decision(topic)` 在 step1 顶部按 LLM mode 改全局并重算整条派生链(含 codex High 补的 `STORYBOARD_GRID_MULTIREF_MOTION/MAIN/SEGMENTS/PRIMARY` 撤回). 手动 mode flag/env(含 ADS_REPORTER_MODE 全别名)优先→跳过自决. 派生链冒烟 6/6. **B64.2 (改 bot, 需大哥 sign-off)**: strip `registry.py` execute_adr schema 的 flag 属性 + 简化 `_execute_adr` 为 `[py,script,topic,fmt]` + collapse `SOUL.md` 触发规则. **必须先 B64.1 默认 ON 实测 ≥3 题材全 mode 绿了, 再改 bot**(否则 bot 停发 flag 而 ADR 没自决稳 → 全默认 HADS). bot 是另一项目服务其它流程, 故 sign-off. **Low 遗留**: cache v5 不校验 mode 缺失(graceful 保持现状, 默认关无影响). 原: 大哥架构洞察: bot registry.py 硬编码关键词决策 ads_reporter/ads_dialogue/bgm_only 跟 ADR 自己的 LLM 团队 (PR-3a topic_decomposer + director_route + 7-Agent brief) 重复, 还容易误判 ("关于记者的题材"→误触发 ads_reporter 视角). 修方向: (1) 复用 _llm_topic_decomposition 链路加 mode 字段 (HADS/HADSD/ads_reporter/bgm_only auto-detect 基于题材+第一句文体) (2) bot registry.py 删 ads_reporter/ads_dialogue/bgm_only 参数, 默认 False, 只透传 topic+fmt 给 ADR (3) ADR 自适应决定 mode. 配 OiiOii 7-Agent 哲学 (内部 LLM 团队全权决策, bot 退到透传层). 工程量 2-3h: mode_decider 1h + bot 改 30min + e2e 测试 1h |
| ~~**B58 Auto-IP voice_asset 推荐 max_tokens 不够**~~ | ★ P3 | — | ✅ **resolved-by-B77 (2026-06-02)** | **就是 B77 修的同一处!** B58 说的 `_llm_pick_voice_asset_for_ip`(L9278) 调用点 = L9316, B77 已把它从 `tier_chat("data", max_tokens=80)`(thinking 模型 Lite, finishReason=MAX_TOKENS) 改成 `chat("GEMINI_25_FLASH", max_tokens=300)`. B58 当年猜"max_tokens 可能低于 4096", 真相是 **80**. 冒烟验过 voice_id 推荐 2.5F/300 → luo_xiang_001 正常返回. 无需再修. |
| **B57 voice_assets 库 user_identified 标签验证** | ★★→**★★★ P1** | 4-6h (含人工) | 🟡 罗翔已清 + **全自动分诊器 shipped 2026-06-03**; 人工耳听 21 个待办 | **2026-06-03 ship `tools/audit_voice_consistency.py`**: ffmpeg+numpy(无 torch/声纹库) 声学特征(MFCC/F0/频谱质心)→全局稳健归一化(median/MAD)→per-asset 绝对阈值 T=6.5 单链聚类. 校准: 同人NN p90=6.0/两两p75=6.16→T=6.5在谷. 全库 100 asset 17s 扫完 → **clean 73 / polluted 14 / even_split 6 / ambiguous 1 / insufficient 6 → 21 待人工耳听, 79 免听(降79%)**. 黄金回归: 罗翔 4 条 surface 出 03/04(2v2 同性近音色粗特征解不了→标交人工, 诚实). **定位=report-first 分诊**(把听全库降成听可疑子集, 不自动删; --apply 占位待启). CCP S1(report-first steer)+S3(2High空保护/Med路径约束已修). **彻底精度需训练声纹 embedding(onnxruntime+wespeaker)列为后续**. 报告 voice_assets/voice_consistency_audit.json. 仍需大哥耳听确认 21 个. **2026-06-02 罗翔实测 = 多音色真根因!**: 沁园春多音色查到 B51 长参考拼了脏数据 → 把罗翔 4 条参考 TG 发大哥听辨 → **ref_03/04 污染(混入别的说话人, diarization cluster 8 切错), 仅 ref_01/02 是真罗翔**. 已删 03/04 改 reference_audios 4→2 + qa_status=verified_clean_ref01_02 (backup .bak_b57luoxiang). **洞察: 多音色根因不只 WERYDANCE 随机, 更是参考数据脏 — B51 拼接还放大了污染(把脏 ref 拼进长参考)**. **全库同病**(罗翔/许知远/楼烨/窦文涛等几十个都从同一 youtube XYMaIn2JEh0 聚类切, 都 user_identified 未核实)→ 系统性扫描清洗刻不容缓, 升 P1. 原: 大哥 123250 反馈"音色一致但不是许知远". root: external_xu_zhiyuan_xyma_001 metadata 是 'user_identified' (用户标注但 qa_status=person_voice_user_identified_not_authorized), 截取自 youtube XYMaIn2JEh0 1364-1373s 9s ref_01.wav 实际可能是节目嘉宾而非许知远本人. 修方向: (1) tools/audit_user_identified.py 扫所有 label_source=user_identified asset (2) tools/play_voice_ref.py 批量播放 + 人工标注 (alias: 真人/疑似/非本人) (3) 错标的清掉 voice_asset_id 或重 source_url 重抓 (4) IP card.voice_asset_id 跟着更新. 影响所有"候选音色"系列 asset (xu_zhiyuan/lou_ye/dou_wentao 等几十个 youtube_extracted_candidate) | 122716 跑 B56 后仍选牧神记. root: _generate_adsd_dialogue_turns 在 _finalize 之前已给 turn["voice_asset_id"] 赋值 (LLM 选牧神记), B56 原 check `not turn.get(voice_asset_id)` 跳过 IP 覆盖. 修 _finalize_adsd_turns: ip_voice 存在则强制覆盖 (IP 是最高优先级), log warning 显示覆盖. 2/2 unit test 验证 turn 已有 voice_asset_id 也覆盖 + 旁白 fallback 保留 LLM |
| ~~**B55 PR-A merged_a 改用 voice_asset clone**~~ | ~~★★★ P0~~ | ~~45min~~ | ✅ shipped 2026-05-29 (CCP Stage 1+4) | 大哥 110947 反馈"音色还是俩". root: PR-A merged_a 走 TTS dub (audios=concat TTS) ≠ per-scene voice clone (audios=voice_asset_ref.path). 100336 走 per-scene 慕神纪 vs 110947 走 TTS 78 → 听感俩. 修: L14563 _b55_use_voice_clone (voice_asset_ref + path exists) → audio_dub_audio=voice_asset_ref.path (慕神纪 8s), fallback 缺失走 TTS dub + log. L14594 prompt 重写: voice_clone 模式用 B49 风格 'speaking exactly these lines: 「turn1」...「turn8」' + 'voice timbre reference / do not treat as content', cap 2000 (实测 3 turn ~444 char). TTS fallback 原 prompt 保留. 2/2 unit test 全过. 跟 per-scene + grid_multiref B49 同思路统一 |
| **B23 ADSD 跨 turn 旁白音色漂移** | ★★ P2 | 1-2h | pending | (原认错诊断, 大哥指出实际是 HADS, B24 已修. ADSD 多人模式跨 turn 同 speaker 是否也漂移待复现确认后再判优先级) — 2026-05-29 B53 确认 ADSD monologue 路径漂移 root cause 跟 B23 合并 |
| ~~**B54 _postprocess_audio_dub_segment in-place guard**~~ | ~~★★★ P0~~ | ~~20min~~ | ✅ shipped 2026-05-29 (CCP Stage 4) | 满江红 105219 实测 B53 ship 后 PR-A merged_a 3 batch 全 ffmpeg fail "Output ... same as Input - exiting" 回退逐 turn (B53 effective 归零). root: _postprocess_audio_dub_segment input=src_video output=scene.vid_path 同名 (PR-A split 写入 seg_N.mp4 当 src). 修 L14089 入口加 guard: `os.path.realpath(src_video) == os.path.realpath(vid_path)` 时 `os.rename` src 到 vid_path + '.b54_inplace_tmp.mp4', src_video 变量更新 (比 copy 快 PR-A split 已产 seg_N.mp4 不再用). 不修 PR-A.1 调用方 (通用 guard 解决任何路径同名场景). 2/2 unit test 全过. 实测重跑验证 B53+B54 联动 |
 满江红 105219 实测 B53 ship 后 PR-A merged_a 触发 ffmpeg "Output ... same as Input #0 - exiting / FFmpeg cannot edit existing files in-place" → 全 3 batch 都失败回退逐 turn (B53 实际效果归零). root: PR-A merged_a 路径 scene["vid_path"] 提前 init 为 seg_N.mp4, _split_lip_sync_raw_by_durations 调 ffmpeg input=scene.vid_path output=同 seg_N.mp4 → 同文件冲突. 修方向: (1) split 前临时输入用 raw_combined_path 而非 scene.vid_path (2) 或先 split 到 tmp 文件再 atomic rename 到 seg_N.mp4. PR-A.1 单测之前 PASS 但用 fake 数据未触发此路径, 真实 run 才暴露 |
| ~~**B53 ADSD monologue 自动启用 speaker_batching**~~ | ~~★★★ P0~~ | ~~30min~~ | ✅ shipped 2026-05-29 (CCP Stage 1+4) | 满江红 100336 反馈"独白音色不统一". root: 8 turn 同 voice_asset (external_mushenji_lingjing_001) + 同 ref_01.wav 但 WERYDANCE almighty 8 次独立 clone stochastic 漂移. PR-A merged_a 已 ship (单 submit batch 多 turn) 但默认 off (--adsd-speaker-batch). 修 L15125 use site: detect script 全 monologue (1 unique speaker) → 局部 effective flag enable batching, _b53_auto_batch + _b53_effective_batching 替代 module 变量. env ADR_ADSD_FORCE_SOLO_LIP_SYNC=1 opt-out. 已显式 flag 跳过 auto. log + tg '🤖 B53 ADSD monologue 自动 batching'. 7/7 unit test 全过 (monologue / 多 speaker / 显式 flag / opt-out / 非 ADSD / 空 / 缺 speaker). 满江红 8 turn → 自动按 15s WERYDANCE 上限合 2 batch (4 turn × 3.5s ≈ 14s), 1 次 submit 替 8 次, stochastic 漂移降 ~75% |
| **B24 HADS 单旁白默认用 master TTS audio** | - | ~~30min~~ | ❌ REVERTED 2026-05-27 | PM 设计认知错误: TTS master_voice.mp3 是用来锚定分镜头时长 (step3 时间轴), 不是听感; **音色库 (voice_asset clone WERYDANCE audio_dub) 才是首选听感**. 我之前 B24 把 HADS 默认强制 master TTS = 完全反了大哥的设计意图. 立即撤回, step9 仍走 embedded audio_dub. 真 root cause 「旁白音色不一致」是 audio_dub fail rate + WERYDANCE clone stochastic 漂移, 应该走修 rate limit / retry 路径 (B26 待评估). |
| ~~**B25 _PODCAST_TO_VOICE_ASSET_MAP pingshu 映射纠正**~~ | ~~★★ P2~~ | ~~15min~~ | ✅ shipped 2026-05-28 (CCP Stage 1+3+4) | L824 `pingshu-c7c18f5a` 映射从 `external_liang_wendao_yt_001` (梁文道, 文化评论) 改 `external_yi_zhongtian_yt_001` (易中天, 评书腔 + 历史讲述), 跟 LLM 选的"古今先生评书风格"匹配 · 库内 asset 校验存在 4 reference audios · 影响所有评书风格题材的 voice_asset clone |
| **B26 audio_dub fail rate 优化** | ★★ P2 (B49+B50 ship 后降级) | 1-2h | pending | 大哥原反馈"旁白音色不一致" 真 root cause 实际是 B40 系列 (audio 链路丢) + B49 (内容不对) + B50 (音色不对). B26 现在只影响 per-scene motion fallback 路径 (非 HADS/VADS 主路径, grid_multiref 是主路径). 162922 实测 B49+B50 听感对, B26 边际收益下降. 修方向 (留待 fallback 路径触发频次升高再做): (1) WERYDANCE submit retry 2-3 次 (2) PR-A.2 rate limit 协同 (3) silent fallback 插 master TTS audio fill |
| ~~**B27 文档校正 voice_asset 首选听感**~~ | ~~★ P3~~ | ~~15min~~ | ✅ shipped 2026-05-27 (CCP Stage 4) | B24 撤回引出的根源: 代码 26+ 处称 TTS 为"主音轨" 误导 PM/dev 以为 TTS 是首选听感, 实际 voice_asset clone (WERYDANCE audio_dub) 才是 · 修 4 点文档措辞 (代码逻辑不动): step2 docstring 加 B27 警告说明真实 audio 链路 / _build_voice_clone_hybrid_audio docstring 改 / step9 use_embedded 前加 inline 注释说明优先级 / sync_note + audio_note 措辞校正 (clone "首选听感" / TTS "fallback 非首选") / ADR 执行计划表 Step2 描述加 B27 标签 · 保留 dashboard pattern 用的"主音轨" tg msg (L1143/1178 regex dependency 不动) |
| ~~**B28 audio_dub timeout 不阻 motion fallback**~~ | ~~★★★ P1~~ | ~~10min~~ | ✅ shipped 2026-05-27 (CCP 全 4 阶段) | 「观沧海」144159 run 7 turn 全 audio_dub timeout 阻断 fallback → 整片 25min motion 阶段烧 0 成果纯 Ken Burns 静态 · root cause: L10689 timeout return (True, False) handled=True 让 caller L10847 短路 · 修: L10688-10696 任何 fail 都 return ok,ok 不 handled, caller 继续走 reference_video / text-to-video fallback · Stage 3 Codex 抓 Medium 注释跟行为不符 (task_id 覆盖丢失 resume) → 注释纠正 · 代价: audio_dub 后台真成功时浪费 1 次 credits (任务覆盖无法 resume) · motion 视频是核心交付价值大于 credits |
| ~~**B29 step65 motion fast-fail**~~ | ~~★★ P2~~ | ~~30min~~ | ✅ shipped 2026-05-27 (CCP 全 4 阶段) | 配套 B28 防 WERYDANCE 上游异常时整 step65 烧 25min · 实施: L13964 `_run_batch` 加 fail_count 跟踪, round 1 fail_count ≥ max(3, 40%n floor) 且 fail_rate ≥ 50% 触发 raise + cancel 剩余 future · CCP Stage 3 抓 High (ThreadPool 限制 cancel 只对未启动 future, 已启动 task 仍跑完) → 文档补说明实际收益 40-60% (省 round 2 + 子 path + step66) · Medium (floor 取整偏激进 fast-fail, 接受) · 6/6 矩阵 (观沧海 fail 3/4 触发 / 2/4 不触发 / 12 turn 多场景) · env ADR_MOTION_FAST_FAIL=1 默认开 |
| **B30 DubResult 状态机重构** | ★ P3 | 0.5-1d | pending | Codex Stage 2 推荐方案 B: 抽象统一 DubResult (ok/timeout/fatal/retryable) 让 B28/B29 都消费状态机, step65 按批次熔断 · 工程量大但 maintainability 好 · 现在先 ship B28+B29 止血, B30 留下波重构 |
| **B32 step2 text-to-audio 路由** | - | - | ❌ REJECTED 2026-05-27 | PM 诊断认知错误连环: (1) 先以为 text-to-audio 用错音色 (实际 master_voice 只锚定时长, 音色不重要 B27 已校正) (2) 再以为参数传错 (对照 WeryAI doc 5 个参数 text/voice_id 78/speed '1.0'/vol '4.0'/trace_id 全部符合 schema) (3) text-to-audio chunk fail 是 WeryAI 上游 transient (跟今天 step65 motion 7 turn 全 timeout 同一服务异常). 现有设计逻辑没问题, 不需要改. 撤回 B32 backlog. |
| ~~**B33 step6 错峰提交 12→5s**~~ | ~~★★ P2~~ | ~~10min~~ | ✅ shipped 2026-05-28 (CCP Stage 1+4) | 3 处默认值 12→5s: L6654 motion_bridge_refs / L6832 storyboard fallback / L8917 storyboard_grid · IMAGE_RATE_LIMIT_BACKOFF 15s × attempt+1 retry 路径不动, 429 触发自动退避 cover · env override 保留可显式回滚 12 · 收益: 单 run ~42s, 7-batch run 1-2min · Stage 4 syntax/mock/regression PASS |
| ~~**B34.1 grid_multiref 拆 group + duration 拉满**~~ | ~~★★★ P1~~ | ~~30min~~ | ✅ shipped 2026-05-27 (CCP 全 4 阶段) | 大哥实测 B34 反馈缺点 2 (切换太快 1.4s/panel) + 缺点 3 (重播 pad 2×) · 修 3 处: (1) L957 ADS_FLOW setdefault `ADR_STORYBOARD_GRID_MULTIREF_GROUP` 12→4 (2) L11122 `_grid_multiref_group_size` 默认 4 (3) L11141 `_grid_multiref_duration` cap 10→15 (WERYDANCE 硬上限) · 🔄 **自适应特性**: almighty 调用数 = ceil(N/4) 跟 turn 总数同比例增长 (5 turn→2, 10→3, 12→3, 20→5 调用), 每 group duration 跟 group 内 sum(turn dur) 自适应 cap 15s; 总片长 = sum(target_dur) 跟内容时长决定不受 group 数影响; 节省 vs per_scene 固定 ~75% almighty 调用 · 7 panel 拆 4+3 两组每组 cap 15s = 3.75s/panel (vs 之前 1.4s/panel), 总 ~25s ≥ audio 16.7s 无需 tpad pad · Stage 3 Codex 抓 High (nan/inf 污染 total → math.isfinite filter) + Low (注释 15s/4 panel 表达) 修 · 6/6 矩阵通过 (env override + cap + edge case) |
| **B34.4 segment_planner 长期重构** | ★ P3 | 1-2d | pending | Codex Stage 2 推荐方案 B 长期: 引入 `segment_planner` 按 `per_panel_sec` + 模型最大时长 + scene/shot 边界动态分组, duration 由组内 panel 数计算. 工程量大但 maintainability 好, B34.1 简化版止血后再考虑 |
| ~~**B34.2 grid_multiref prompt panel-distinct**~~ | ~~★★ P2~~ | ~~30min~~ | ✅ shipped 2026-05-27 (CCP Stage 1+2+3+4, Stage 4 timeout 本地精算 confirm) | 大哥实测缺点 4 (镜头语言相似 + 背景换无感) · 修 3 处 L11161-11210: (1) Shot line label 改 'Shot N: visual | Cam: motion' 简洁 (2) style 段加 ★ CRITICAL distinct 强约束 (vary angle/distance/lighting/framing/energy 无两 shot 同 camera language) (3) visual trim 180→80 + motion trim 120→120 防 prompt [:2000] cap 截断 · 满载 4 panel + character_sheet 算 1931 char ✓ · CCP Stage 3 Codex 抓 prompt cap 截断 → 3 次精简到 1931 char · Stage 2 替代方案 B (panel_signature 结构化) 入 B34.5 backlog |
| **B34.5 panel_signature 结构化** | ★ P3 | 1-1.5h | pending | Codex B34.2 Stage 2 替代方案 B: 引入 `panel_signature` 生成器为每格分配镜头类型/景别/光向/构图重心/运动强度, 注入 prompt + 渲染前去重校验 · 比 B34.2 文字强约束更可靠, 但工程量大留作长期 |
| ~~**B34.3 grid_multiref 切片对齐 panel boundary**~~ | ~~★★ P2~~ | ~~30min~~ | ✅ shipped 2026-05-27 (CCP 全 4 阶段) | 大哥反馈缺点 2 (切换太快) + 缺点 4 (镜头单调) 都跟 panel 不完整有关 · 修 L11435-11465 `_apply_grid_multiref_segments`: source_start/dur 改 raw_dur/N 等距 panel boundary (不再 target_dur 比例切), ratio = target_dur / panel_dur 计算拉伸, 加 panel_aligned=True audit · 尾段 clamp 防超 raw_dur · mock case 通过: 7 panel×15s + 2.4s/turn → ratio 1.12 ok; 4 panel + 2.4s/turn → ratio 0.64 slow down 给气势充裕 · max_stretch=2.5 保护不变 · Stage 2 替代方案 B (PanelBoundaryMapper) 入 B34.6 backlog |
| **B34.6 PanelBoundaryMapper 长期重构** | ★ P3 | 1-2h | pending | Codex B34.3 Stage 2 替代 B 长期: 新增 PanelBoundaryMapper 以 panel 边界表为唯一事实源, 切片前生成单调 source→target 映射, 所有 turn 查映射不重算 · 可审计 + 可测试性好, 短期 B34.3 简化版止血后再考虑 |
| ~~**B35 grid_multiref 加 voice_asset audio ref**~~ | ~~★★★ P1~~ | ~~30min~~ | ✅ shipped 2026-05-28 (CCP 全 4 阶段) | 大哥实测 B34 反馈"音色不是 LLM 定的, almighty 调用没传 voice ref?" · root: grid_multiref submit 完全没传 audios + generate_audio=false silent → step9 mux master TTS hardcode 78 女声 听感跟 LLM 选 voice_asset 无关 · 修 L12235 _generate_grid_multiref_motion_segments: 加 `voice_ref = _select_voice_asset_reference(group[0])` + audio_url 上传 + payload audios + generate_audio='true' · 3 层 fallback: combo exception (4xx/5xx) → 回退 silent retry / 200 no task_id → 回退 silent retry / 没用 audio 时上游异常 raise 走外层 · audit log voice_ref_used / generate_audio_mode / b35_fallback_reason · Stage 3 Codex 抓 Medium (4xx exception 没 silent retry → 修 try/except 包 inner) · ADSD 路径不变, 仅 HADS/VADS grid_multiref 受影响 |
| ~~**B36 step9 结尾仓促 tail buffer**~~ | ~~★★ P2~~ | ~~15min~~ | ✅ shipped 2026-05-28 (CCP Stage 4 微改) | 大哥实测反馈"旁白刚说完就没了" · root: step9 已有 tail buffer 1.5s 但不够明显 · 修 L15643 `_tail_buffer` 变量, 默认 1.5→2.5s (env ADR_TAIL_BUFFER_SEC override 0-5s) · 4 处 ffmpeg `apad=pad_dur=1.5` 替换为 f-string `apad=pad_dur={_tail_buffer}` 保持视频/音频 tail 同步 · BGM-only / use_embedded / master TTS 三个 mux 路径都修 |
| ~~**B37 storyboard_grid 还原题材具体场景**~~ | ~~★★ P2~~ | ~~1h~~ | ✅ shipped 2026-05-28 (CCP 全 4 阶段) | 大哥反馈"画面没还原曹操写诗环境, 编剧功力不到位" · 修 jiangwen_prompt L3843 输出 JSON schema 加 `scene_anchor` 字段 + 强约束 LLM 给每句 8-15 词英文具象场景 (e.g., "rocky carved cliff at the edge of dark sea") 而非通用风格 · L4003 Python 兜底检测 scene_anchor 关键词嵌入 prompt 率, 缺则 append "scene anchor: xxx" · 跟 B14 anchor 互补 (B14 中文 verbatim core terms, B37 英文具象场景) · Codex Stage 2 推荐方案 B (scene_blueprint 上游分镜) 入 B37.1 backlog · 古装/历史题材忠于原文意象, 现代抽象题材具象物件场景 |
| **B37.1 scene_blueprint 上游分镜重构** | ★ P3 | 2-3h | pending | Codex B37 Stage 2 替代 B 长期: 新增 `scene_blueprint` 预处理按题材+原文生成 7-12 个场景节拍, 直接替换/约束 storyboard_grid, 再让 jiangwen 只细化镜头 · 比 B37 prompt 强约束更可控可审计, 但改动面大留作长期 |
| **B38 panel-distinct 升 panel_signature** | ★ P3 | 1-1.5h | pending | 大哥实测反馈"镜头语言初级青涩" · B34.2 prompt 文字层强约束 distinct 不够, WERYDANCE 实际按整 grid generic 渲染 · **修方向**: 跟 B34.5 (panel_signature 结构化) 合并, 显式给每 panel 分配镜头类型/景别/光向/构图重心/运动强度, 注入 prompt + 渲染前去重校验 |
| ~~**B39 封面异步 worker fail retry**~~ | ~~★★ P2~~ | ~~20min~~ | ✅ shipped 2026-05-28 (CCP 全 4 阶段) | L16957 `_async_kickoff_cover_caption._worker` 加 retry 框架: max_attempts=3 (1 初次 + 2 retry 退避 15s/30s), cover_path=None 也算失败 raise. caption 单独 try fail 用 (topic/topic[:8]/[]) 兜底防 step10 len(None) crash · L17005 `_await_async_cover_caption` 超时置 aborted=True, worker retry 前 check 跳出防竞态写同名 cover.jpg · 加 job_status/retry_count/last_error 字段预留 B30 状态机 · Stage 3 codex 抓 4 issue: High1 caption fail 兜底 / High2 worst case timeout 注释纠正 / Medium3 aborted 防竞态 / Low4 last_error 统一语义 (删 error 字段) · Stage 4 syntax PASS + mock unit PASS + 同步 ROADMAP/ship_log |
| ~~**B49 grid_multiref prompt 嵌入真台词 (voice/字幕同步)**~~ | ~~★★★ P1~~ | ~~30min~~ | ✅ shipped 2026-05-28 (CCP Stage 1+4) | 143918 实测大哥反馈"旁白跟字幕各搞各的". root: _grid_multiref_prompt L11197-11247 漏嵌入台词文字, almighty generate_audio=true 拿 voice_ref 8s + 纯视觉 prompt → 自由发挥说跟字幕无关内容. 字幕来自 master_voice TTS (真台词) + ASR + LLM 断句, 二者各搞各. 修: 仿 _motion_audio_dub_prompt L10569 'speaking exactly this line' 套路, 收集 group 内 scene.text 真台词嵌入 narration_block 'speaking exactly these lines in order: 「line1」「line2」...'. visual 80→50 / motion 120→80 给 narration 腾空间. 实测 prompt 1484 char < 2000 cap. per-scene 路径早就用这套路 work, grid_multiref 漏了 |
| ~~**B40.3 grid_multiref group fail 自动 retry**~~ | ~~★★★ P1~~ | ~~45min~~ | ✅ shipped 2026-05-28 (CCP 全 4 阶段) | 114735 实测 root cause: WERYDANCE group 2 poll #91 task_failed (stochastic 上游异常), success_count 1/2=0.5 < ADR_GRID_MULTIREF_MAIN_MIN_PASS_RATIO 0.75 → combined.mp4 不写 → step7 fallback per-scene → raw_concat 无 audio → master TTS 女声 (B40/B40.1/B40.2 全失效因 root 在 group fail) · 修 L12333 之后加 retry block: transient reason (task_failed/task_timeout/poll_timeout) 60s 退避后复用 payload 重 submit+poll 一次, b40_3_retried 防 inf loop, b40_3_retry_task_id 保存防 process kill 丢任务 · Stage 3 codex 抓 High (submit_without_task_id 早 continue, 死代码 → 移除) + Medium (resumed 分支不 retry → backlog) + Low (json write 非原子, 接受) · 跟 B26 audio_dub fail rate 同思路, 预期覆盖 90% stochastic fail |
| ~~**B40.2 step7 STORYBOARD_GRID_MULTIREF_MAIN 保留 audio**~~ | ~~★★★ P1~~ | ~~30min~~ | ✅ shipped 2026-05-28 (CCP Stage 1+4) | 104140 实测 B40.1 仍未触发 root cause: step7 L14587/L14597 两 ffmpeg 都带 -an 显式 strip audio (B35 前 silent 时代遗留), raw_concat 无 audio → B40.1 _has_audio_stream False → fallback master TTS · 修: pad>0.2 用双 -i (video stream_loop + audio 不 loop) + apad 静音补尾防 voice 重复; pad<=0.2 直拷 audio 重编码 aac; combined 无 audio 时仍走原 -an 兼容 B35 silent fallback · B40/B40.1/B40.2 三连闭环, raw_concat 现在含 voice clone audio → step9 use_embedded=True 路径生效 |
| ~~**B40.1 step9 use_embedded 加 grid_multiref 主路径检测**~~ | ~~★★★ P1~~ | ~~30min~~ | ✅ shipped 2026-05-28 (CCP Stage 1+4) | 观沧海 101437 实测 B40 ship 后仍出 'NoneType.get' crash + fallback master TTS 女声 · root: step9 L15620 use_embedded 检测从不知 grid_multiref 主路径, 只读 motion_qa.json (per-scene, grid_multiref 模式不生成) + lip_sync_qa.json (ADSD) → None.get 炸 → 整 B40/B35/B25 链路 audio 浪费 · 修: L15616 grid_multi_qa 优先分支 (pass=True + raw_path 有 audio + 至少 1 group generate_audio_mode='true' → use_embedded=True, mode=grid_multiref_audio_dub_N_groups); 顺手 motion_qa.json 和 lip_sync_qa.json fallback `or {}` 防同款 NoneType · B43 配套补 L3088 沉浸感 QA 改 tier_chat('review') 真正走 Flash 2.5 (上一版 _LLM_TIER 改了但调用点直连 Flash Lite 没生效) |
| ~~**B40 grid_multiref concat normalize 丢 audio**~~ | ~~★★★ P1~~ | ~~20min~~ | ✅ shipped 2026-05-28 (CCP Stage 1+3+4) | 大哥实测反馈"听感不是易中天也不是梁文道而是女声 + BGM 非常小声" · root: L12437 `_grid_multiref_concat_paths` normalize 阶段用 `-an` 显式 strip audio (B35 前 grid_multiref silent 无所谓), B35 ship 后 group 视频含 voice_asset clone audio 被丢 → combined.mp4 silent → step9 `use_embedded_dialogue_audio` 检测无 audio → fallback master TTS hardcode 78 女声 + 走 master TTS 路径 BGM weight 0.4 看起来小声 · 修: 检测 `_has_audio_stream(p)`, 有 audio 重编码 aac 48k mono 统一让 concat demuxer 接受; 无 audio 保留 -an · ship 后 combined.mp4 含 audio (易中天 clone) + step9 走 use_embedded 路径 BGM weights 0.4 比 master 路径 0.4 实际 voice 1.0 反而 BGM 占比对称 (大哥应感觉 BGM 正常) |
| ~~**B34 grid_multiref_motion 升 ADS 主路径**~~ | ~~★★★ P1~~ | ~~1h~~ | ✅ shipped 2026-05-27 (CCP 全 4 阶段, 灰度 B 版) | 大哥洞察: ADS 真正高质路径是 grid_multiref (整 grid 一次喂 WERYDANCE 出长视频 + 切回 N seg) · 实施: L228 `GRID_MULTIREF_PRIMARY` 常量 (`--grid-multiref-primary` CLI / `ADR_GRID_MULTIREF_PRIMARY` env), 启用时连带 MOTION/MAIN/SEGMENTS 全 True + setdefault `ADR_UNSAFE_ALLOW_GRID_MULTIREF_PROPORTIONAL_SPLIT=1` (opt-in 比例切分相邻 panel 风险) · L14025 stats audit 写 `b34_grid_multiref_primary_stats.json` (scene 维度 + group 维度 + seg_reason + unsafe flag) · Stage 3 抓 Medium 2 修: seg_qa.reason 真实字段名 + group_combined_pass_ratio (group 维度真实决策) · 默认 OFF 灰度收数据, B34.1 数据达标后默认 ON · 跟 B31 协同: 主路径 grid_multiref 自带 visual continuity 不用 meta_grid, fallback per_scene 才用 meta_grid |
| ~~**B31 HADS/VADS 也用 meta_grid 召唤**~~ | ~~★★★ P1~~ | ~~1h~~ | ✅ shipped 2026-05-27 (CCP 全 4 阶段, A 灰度版) | 大哥洞察: HADS/VADS 跨 scene 视觉一致性 (观沧海曹操) · 实施 3 处 (env ADR_HADS_USE_META_GRID=1 灰度): (1) L9411 meta_grid_active 扩 HADS WITH_MOTION 条件 (2) L3993 step1_script 用 `_adsd_role_candidates(topic)[0]` 抽 visual_subject 注入 item.speaker (复用 decomp cache, 首跑 1 LLM 调用) (3) L10619 `_try_motion_audio_dub_video` 加 meta_grid_url priority 0 ref + 新 `meta_grid_instruction` prompt 标识 "image 1 = identity grid (not motion frame), image 2 = main motion keyframe" · CCP Stage 3 抓 3 个 High/Medium: skip_storyboard_grid 改 _adsd_meta_grid only (HADS 保留 panel 切片) / bridge_instruction 跟 grid priority 冲突 → 加 meta_grid_instruction / LLM cost 注释纠正 · 16-case mock 通过 (ADSD/HADS/env/skip 全覆盖) · ADSD 路径不变 · 待 ADR_HADS_USE_META_GRID=1 实测跨 scene 一致性效果 |
| ~~**B18 B11 命名迁移漏的 legacy meta_grid fallback**~~ | ~~★★ P2~~ | ~~20min~~ | ✅ shipped 2026-05-27 (CCP Stage 3+4) | B11 (5/26) 给 meta_grid 缓存加 _h/_v suffix 后, 老 `旁白.png` (无 suffix) 在新 lookup 下永远 miss · 旁白 usage=29 / 孙悟空=7 / 科比=6 / 唐僧=4 / 至尊宝=3 每 ADR run 都重新生成 (浪费 GPT_IMAGE_2 ~30-60s ×5) · 修: L8186 新增 `_character_meta_grid_cache_legacy_path` helper, L8224 cache lookup 主路径 miss 时横屏走 legacy fallback (默认无 suffix=横屏), 命中后 copyfile migrate 到 _h 命名让下次走主路径 · 5/5 dry-run 通过 · 每 run 节省 2.5-5min + GPT cost |
| ~~**B15 asr_qa.json 缺失 warning**~~ | ~~★ P3~~ | ~~30min~~ | ✅ shipped 2026-05-27 (CCP Stage 4 only, 微改例外) | ASR endpoint 失败/返回空时 asr_qa.json 不写, delivery_qa 发 warning 污染列表 · 修: L5308 `_asr_verify_dialogue_audio` 后 strip speech_text, 失败/全空白时落地占位 `asr_qa.json {pass: True, skipped: True, reason: ...}` · delivery_qa _qa_file_pass 读 pass=True 不发 warning · Stage 4 抓 "   " 全空白 truthy bug → 加 strip · 17 行 |
| ~~**B16 meta_grid 漏入 audio_dub postprocess**~~ | ~~★★★ P1~~ | ~~30min~~ | ✅ shipped 2026-05-27 (CCP) | 「南京学区房」HADSD t=85.0-85.6s 出 4×3 meta_grid 满帧 · root cause: `_postprocess_audio_dub_segment` L12515 panel_pad 用 scene.img_path = `meta_grid_李薇.png` 当 Ken Burns still · 修: L12393 + L12521 加 is_meta_grid_panel guard (basename.startswith("meta_grid_"))，use_panel_pad 加 `not is_meta_grid_panel`，走 tpad clone 末帧兜底 · 顺手把 B2 (L13950/L13955) 旧 in 检查也对齐 startswith, 4 处统一 · CCP Stage 3 + Stage 4 通过, 6 case mock 矩阵 (lip/audio × meta_grid/non-grid × env on/off) |
| ~~**B17 ensemble 上限按题材动态切**~~ | ~~★★ P2~~ | ~~1h~~ | ✅ shipped 2026-05-27 (CCP 全 4 阶段, 二次修) | 「南京学区房」5 真人 ensemble 触发 delivery fail (硬上限 4) · 修 3 处: (1) L2143 `_ENSEMBLE_CAP_BY_ROUTE` 映射 (modern_documentary/gritty_kinetic → 6, 其它 5 个 route → 5; cap 含旁白配额) + `_ensemble_speaker_cap` helper, decomp 缺 route 默认 5 保守 · (2) L14776 delivery_qa `too many ADSD speakers` + ensemble shape 校验都用 dynamic cap (从 pipeline_state.json 读 topic) · (3) L2402 `_generate_adsd_dialogue_turns` role_candidates 裁剪到 cap, LLM 看不到超 cap 候选 · Stage 3 抓 Medium (decomp 缺 route fallback 不当默认 modern → 修) + Low (delivery 触发 LLM/cache → 接受, 有 cache) · Stage 4 抓初版 cap=4 旁白+4 真人=5 仍 fail → 二修 cap 含旁白配额 现代 6 / 古风 5 · 7/7 矩阵 (NBA=6/笑傲=5/古风 6 拒/缺 route=5/monologue=1) |

---

## 📋 协同 SOP · CCP v1.0 (2026-05-26)

**CCP (Claude × Codex Protocol)** — 让任何 Claude session 跟 codex 全链路协同, 不再单向 ship 后才审.

| Stage | 触发 | 工具 |
|---|---|---|
| 1 review-spec | 收到需求/bug | `tools/codex_collab.py review-spec --topic ... --context ...` |
| 2 propose-alt | 出方案 A 后 | `tools/codex_collab.py propose-alt --topic ... --my-plan ...` |
| 3 review-code | 改 3+ 文件 / 100+ 行 | `tools/codex_collab.py review-code --files ... --topic ...` |
| 4 final-qa | ship 完 | `tools/codex_collab.py final-qa --topic ... --files ...` |

**例外** (跳过 Stage 1-2): 5 行内 / 单文件 typo / 改文档.

**持久化**: 每次协同写 `voice_assets/collab_history.json` 跨 session.

**约束** (写进 feedback memory + CLAUDE.md): Claude 收到 ADR 需求自动走 CCP, 不能拍脑袋.

---

## ✗ Deprecated (撤回方向)

| 方向 | 撤回原因 | 数据来源 |
|------|----------|----------|
| 12-panel grid micro-storyboard | Spike 3C 输出综合场景非按 panel 顺序 | Spike 3 |
| audio_ref 拼接 (>15s) | API 硬性限制 2-15s | Spike 1 |
| step6/step66 流水线化 | 收益 < 5min 不值得复杂度 | 之前 PM 分析 |
| meta_grid 2K 替代 4K | 质量明显降级 | 经验判断 |
| WERYDANCE 高优先 GPU quota | 需付费，非项目内 | - |

---

## 📌 PM 注释

**Session 重启 protocol:**
1. Claude 启动时先读 `memory/project_adr_backlog.md` 看 top 3 优先级
2. 再读这个 ROADMAP.md 看全量 backlog
3. 跟用户对齐当前方向（不直接跳进未做项）

**优先级判断标准:**
- ★★★ ROI 高 + 工程量 < 3h
- ★★ 中等 ROI 或工程量 3-5h
- ★ 长期价值或工具型

**当前 top 3 (2026-05-25 更新):**
1. **达尔文 Round 2** (3h) — 阶段 C 题材分类器 + 阶段 E 自动 IP 进化
2. **PR-3 专业角色补全** (4h) — 武术指导/美术指导/剪辑师/音效师
3. **PR-A · merged_a 合并跑** (4-5h) — 大杀器，省 8-12min/run

**5/21-5/25 累计 ship 32 件 (含今日 7 件):**
  · IP 系统 7 件 (A 孵化 / AUTO-IP / B 自学习 / C 关系 / F 统计 / I 版本 / 占位符过滤)
  · LLM 化 6 件 (topic_decomposition / BGM / role / 武戏密度 / 智能召唤标签 / action_b 标注)
  · Bug 修复 8 件 (duration=3 / step7 防御 / B1-B6 / Ken Burns)
  · 进化基础设施 3 件 (B4 武戏 SFX / FAST retry / task_failed 诊断 / meta_grid 重构 / quality_audit / 旧 grid 清理)

零工 / 已可立刻用：
- 跨题材召唤 (用 IP 即可)
- LLM 智能召唤标签 (现在 B 已 ship，可做)
